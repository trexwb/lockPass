# 2026-08-27

## v1.0.3 — 修复备份 JSON 导入报「不支持的文件格式」

**现象**：批量导入弹窗选择自动快照 / 同步生成的 JSON 备份（`backups/LockPass-backup-*.json`、`LockPass-vault.json`）时，右下角 Toast 报「不支持的文件格式」。

**根因**：加密备份有两套信封，导入侧只认其一——

| 文件 | format 标记 | 结构 |
|------|------------|------|
| `.vault` 手动导出 | `encrypted` | salt + iterations + iv + data |
| 自动快照 / 文件同步 | `LockPass-file-sync` | salt + iterations + iv + data（同构） |

- `ImportModal.previewVault()` 仅接受 `format === 'encrypted' && data.data` 或明文 `entries`；快照 JSON 两条都不命中 → 落入 else 报错
- 主窗口拖拽导入 `import-bridge.js processFile()` 同样的 `format === 'encrypted'` 判断，同样漏掉快照

**修复**：两处均改为按加密封套结构识别——文件同时含 `salt + iv + data` 即走主密码解密流程，不再依赖 format 标记；解密逻辑不变（PBKDF2 按文件自带 salt/iterations 派生）。预览展示时间回退读取 `updatedAt`（快照无 exportedAt）。

**改动文件**：
- `src/components/modals/ImportModal.vue` — previewVault 加密封套结构判断
- `src/core/import-bridge.js` — 拖拽导入同一兼容
- `docs/spec.md` §3.11 — 格式表补 `.json` 行 + 识别方式说明
- `README.md` — 导入小节补快照说明；更新日志新增 v1.0.3

**验证**：
- 谓词行为测试：vault 导出 / 快照 / 明文 entries / 垃圾 JSON / 旧版无 iterations 五种形态分类全部符合预期
- `npm run vite:build` 通过
- `npm run version:check` 全部 11 处一致

**版本**：v1.0.2 → v1.0.3（PATCH，Bug 修复），`npm run version:set 1.0.3`

**注意**：改动留在工作树未提交，由用户决定是否 commit。

## v1.0.4 — 回滚功能优化：「密码历史」升级「修改历史」

**需求**（用户 4 点）：① 除密码外任意修改都生成记录；② 点击回滚先确认防误操作；③ 回滚成功删除被执行记录防重复执行；④ 回滚不新增记录（回滚不是编辑）。

**数据模型变更**：
- 旧：`history[id] = [{ password, at }]`（仅存旧密码）
- 新：`history[id] = [{ at, snap, fields }]` —— snap 为修改前可回滚字段深拷贝（title/entryType/username/password/url/port/notes/tags/root/appId/privateKey），fields 为本次变更字段列表；旧版仅密码记录兼容（只恢复密码，展示「旧版记录 · 仅密码」）

**实现要点**（`src/composables/useVault.js`）：
- `recordEntryHistory(id, prev, next, at)` 替代 pushHistory：diffKeys 全字段比较（histEq 深比较数组/对象、nullish 归一），有差异才入史；与最新一条快照全等去重；上限仍 5
- `rollbackEntry(id, at)` 替代 rollbackPassword：先 `window.Utils.confirm` 确认弹窗（danger，提示覆盖字段与不留副本）→ 应用快照 → 删除被执行记录（空则删 key）→ 单次 saveVault 原子落盘；**不再回写当前状态为新记录**
- `snapDiffers(entry, snap)`：回滚按钮禁用态 + 同版本守卫（旧版记录只比密码）
- favorite/showPassword 状态位不入史
- 导出 API 更名并新增 snapDiffers / describeHistoryFields

**UI**（DetailPanel.vue）：区块改名「修改历史」，每行 = 时间 + 掩码密码 + 变更字段说明；entries.css 加 .history-changed。

**验证**：16 项行为模拟断言通过（首轮 2 个失败为测试脚本断言笔误，修正后全绿）；vite:build 通过；version:check 11 处一致。

**版本**：v1.0.3 → v1.0.4。⚠️ 按规范本变更属功能增强、MINOR 需用户决定；AI 仅自增 PATCH（1.0.4），如需 v1.1.0 一句话即可 version:set。

**文档**：spec §3.12 整节重写、README 功能清单行改写 + 更新日志 v1.0.4。

## v1.0.5 — 修复每次写入都报「本地文件同步失败：e.getFileHandle is not a function」

**现象**：任何写入操作（保存条目等，doSave 每次都触发 FileSync.syncNow）弹红色 Toast「本地文件同步失败：e.getFileHandle is not a function」，保存本身成功。

**根因**：存储的目录句柄退化为无方法普通对象。FileSystemDirectoryHandle 无法经 JSON 序列化还原（桌面版 DBUtilsFile 的 meta.json / 任何文本迁移场景会把句柄写成 `{kind,name}`），引擎升级也可能使旧句柄失配；syncNow 照常调用其 getFileHandle → 抛错 → 每次写入必弹 Toast 且永不自愈。

**修复**：
- `file-sync.js`：新增 `isUsableDirHandle()` + `ensureUsableDirHandle()` 自愈式获取——句柄无 getFileHandle 方法时自动解绑（clearDirHandle + 清 lp_sync_bound）、warning Toast 仅一次，返回 null 让后续写入按未绑定静默跳过；syncNow 改走自愈入口
- `backup.js` 浏览器快照分支同样改走自愈
- `SettingsModal.vue`：状态区识别失效态显示「目录句柄失效，请重新绑定」/「句柄失效 · 需重绑」；settings.css 补 .tag-warning

**验证**：8 项行为断言通过（正常/损坏/清除后静默/未绑定/重绑恢复）；vite:build 通过；version:check 一致。

**版本**：v1.0.4 → v1.0.5（PATCH，Bug 修复）

## v1.0.6 — Windows 绑定目录问题加固（用户补充复现：win 下绑定软件根目录后必现）

**复现链路**：Windows 桌面运行 → `window.__TAURI__` 注入异常（file-store 早退）→ 桌面被误判为浏览器 → 设置面板出现「绑定目录」入口 → 用户绑定后 WebView2 句柄经 JSON 落盘 meta.json 丢光方法 → 每次写入 syncNow 报 getFileHandle not a function。

**加固**：
- `file-store.js`：`__TAURI__` 缺失时用 `__TAURI_INTERNALS__.invoke` 兜底桥接（Tauri v2 IPC 底层入口恒在），桌面永远走文件存储模式，绑定入口不再暴露
- `file-sync.js bindDirectory()`：Tauri 环境硬拒绝（双信号 FileStore.isTauri / __TAURI_INTERNALS__），提示「桌面版数据已由本地文件自动保存，无需绑定同步目录」
- `SettingsModal.vue`：两处桌面判定同步升级为双信号

**验证**：6 项环境分支断言通过；vite:build 通过；version:check 一致。

**版本**：v1.0.5 → v1.0.6。已损坏状态的清除依赖 v1.0.5 ensureUsableDirHandle 自愈（首次写入自动解绑 + 提示一次）。

## v1.0.7 — Windows 桌面启动 404（需刷新）复发修复 + Tauri 环境判定统一收口

**现象**：Windows 桌面版打开显示 Edge 风格 404 错误页（找不到 http://tauri.localhost/），点「刷新」才能进入——即 v1.0.2 修过的问题复发。

**统一根因（今日三轮问题串成一条线）**：该用户 Windows 环境 `window.__TAURI__` 全局注入缺失。
- sw-register.js 单信号判定失效 → tauri.localhost 上注册 PWA SW → 旧 SW 拦截首屏 → 404
- file-store.js 同判失效 → 桌面伪装浏览器 → 绑定入口暴露 → 句柄 JSON 落盘退化 → v1.0.5 的 getFileHandle 报错
- 即：v1.0.3~v1.0.6 五轮其实都在收拾这一个根因的不同后果

**修复**：
- 新增 `src/core/tauri-env.js`（main.js 最先 import）：双信号探测，`__TAURI__.core.invoke` 直连，缺失时回退恒存在的 `__TAURI_INTERNALS__.invoke` 并警告，挂载 `window.LockTauri`
- 四个消费方全部收口读 LockTauri：file-store / sw-register / tauri-bridge / tauri-server-bridge
- sw-register：桌面清残留提前到脚本求值期执行（不等 load），缩小旧 SW 干扰窗口；browser 分支逻辑不变
- 桥内 `T.webview/T.event` 等 UI 增强改为可选感知原生全局，兜底模式下优雅跳过

**验证**：环境分支断言 6/6；vite:build 通过；version:check 一致。

**版本**：v1.0.6 → v1.0.7

**给用户的关键提示**：装上本版后第一次打开若仍 404，手动刷新一次（清残留需要一次应用运行期）；之后每次启动正常。若需彻底一次性恢复，可先卸载重装（或清除 WebView2 用户数据目录）。

## v1.0.7 附加 — Pages 流水线随站发布浏览器扩展 zip（纯 CI 配置，不升版本号）

- `.github/workflows/pages.yml`：dist 校验后新增「打包浏览器扩展 zip」步骤——从 extension/manifest.json 取版本号，`zip -r dist/lockpass-extension-v<版本>.zip extension/. -x '*.DS_Store'`，随 Pages 产物一起部署
- 部署后地址：https://trexwb.github.io/lockPass/lockpass-extension-v<版本>.zip
- README 同步：扩展安装节改为双方式（下载 zip / 源码目录），在线版小节补 zip 地址
- 本地验证：YAML 解析通过 + 按同命令实打包成功（manifest.json 位于 zip 根，解压即可加载已解压扩展）

## v1.0.8 — 设置面板新增「浏览器扩展」下载入口

**需求**：系统内提供扩展 zip 下载（指向 Pages 发布的 lockpass-extension-v<版本>.zip）+ 使用指南链接。

**实现**（SettingsModal.vue，备份分组后新增「浏览器扩展」分组）：
- 「下载扩展包」：URL = https://trexwb.github.io/lockPass/lockpass-extension-v{APP_VERSION}.zip（构建期注入版本号，与 extension/manifest.json 由 version:set 同步）
- 「使用指南」：指向 GitHub 文档 docs/lockpass-扩展使用指南.md
- openExternalUrl 平台分支：浏览器 window.open(_blank,noopener)；桌面版未接 shell/opener 插件 → 复制链接到剪贴板 + Toast 提示，并提供说明文案行
- 复用既有 appVersion computed（'v1.0.x' 带前缀），曾引入重名声明已修正

**验证**：vite:build 通过 ×2；version:check 一致。

**版本**：v1.0.7 → v1.0.8（UI 功能点；如按功能增强可升 v1.1.0，由用户决定）

## v1.0.9 — Windows 404 根治（受控对比锁定根因）+ 桌面数据迁移桥

**决定性证据（用户补充）**：同机另一 Tauri 项目 fastenerTradeWorkbench 从不复现。配置逐项对比后唯一结构差异 = lockPass 有 PWA SW（src/public/sw.js + 全版本注册），fastener 无；两者 identifier 不同 → WebView2 数据目录完全隔离 → 历史残留只属于 lockPass 自己。

**根因定案**：http://tauri.localhost origin 上存在历史注册的 Service Worker（早期构建注册成功过），冷启动首次导航由其接管时产生浏览器级 404；网络优先策略下为何仍 404 = SW 上下文内 fetch 自定义 scheme 主机与主框架行为不一致的 WebView2 已知风险面。刷新能好 = 第二次导航绕开故障态且会话内清理逻辑得以执行，但每次冷启动循环重演。

**v1.0.9 落地四层**：
1. `scripts/remove-dist-sw.mjs`：桌面包剥离 dist/sw.js（beforeBuildCommand 链尾）——未来不可能再注册；浏览器版/Pages 不受影响
2. `src/core/boot-flag.js`（最先加载同步置 __LOCKPASS_BOOTED__）+ lib.rs setup 注入探针线程（500/1400/3000ms eval）：错误页上下文内自动 reload ≤2 次，替代手动刷新
3. file-store 一次性迁移桥：文件空而旧 origin IDB(PasswordVaultDB) 有 salt+vault.main → 整体搬入 meta.json/vault.json；排除 dirHandle；openDB 闸门串行化防竞态 —— 这同时修掉了「__TAURI__ 异常期桌面密码库一直躺在 IDB、file-store 启用后会显示空库」的数据完整性隐患
4. SettingsModal 桌面外链改走 Rust open_url（协议白名单），剪贴板降级兜底

**验证**：cargo check 通过（Rust 改动真编译）；迁移谓词断言 7/7；vite:build 通过；tauri.conf.json JSON 校验通过；version:check 一致。

**版本**：v1.0.8 → v1.0.9

## v1.0.11 — 桌面版自动更新上线（08-28）

**需求**：GitHub 打包的应用自动升级。

**实现（Tauri 官方 updater 插件全链）**：
- 依赖：tauri-plugin-updater/-process "2"；lib.rs 注册；capabilities 加 updater:default/process:default
- 签名：tauri signer 生成密钥对（空密码，CI 免配 PASSWORD），私钥 ~/.tauri/lockpass-updater.key（用户须自行录入 GitHub Secrets TAURI_SIGNING_PRIVATE_KEY），公钥内嵌 tauri.conf.json plugins.updater.pubkey
- 端点：releases/latest/download/latest.json；bundle.createUpdaterArtifacts 生成签名更新产物（NSIS setup.exe / macOS app.tar.gz + .sig）
- CI：release.yml 双构建 job 注入签名变量 + 上传 .sig/.app.tar.gz；新增 update-manifest job（download-artifact → scripts/gen-latest-json.mjs → latest.json 随 Draft Release 上传）
- 前端：core/updater.js 桥（启动 5s 静默检查 → 自动下载安装 → Toast+确认重启 → process.relaunch；设置开关 lockpass_auto_update 默认开）；SettingsModal 关于分组新增「应用更新」状态行（检查/下载进度/重启）+「自动检查更新」开关（仅桌面显示）

**验证**：cargo check 通过（新插件编译）；gen-latest-json 冒烟测试（双平台 fake 产物 → JSON 结构/版本/URL 正确）；release.yml+pages.yml YAML 合法；vite:build 通过；version:check 11 处一致。

**版本**：v1.0.10 → v1.0.11

**补记（08-28 v1.0.13）**：本地打包缺签名变量报错修复——tauri:build 接包装器 with-updater-key.mjs（自动注入本地私钥路径 + 显式空密码）；release.yml 两构建 job 补 TAURI_SIGNING_PRIVATE_KEY_PASSWORD=""（无 TTY 环境空密码密钥必须显式空值，实测签名冒烟通过）。期间发现工作区存在并行编辑（版本号/文件被外部重置），已完成版本收敛 v1.0.13。

**用户侧待办**：① GitHub Secrets 添加 TAURI_SIGNING_PRIVATE_KEY（本地 ~/.tauri/lockpass-updater.key 全文）；② 推 v1.0.11 标签发布并 Publish Draft；③ v1.0.11 为引导版本，旧安装需手动升级一次。

补记：README 中 v1.0.10 日志条目被外部移除（用户整理），不再复活；v1.0.11 条目已按当前锚点插入并含 v1.0.10 摘要行。

## v1.0.12 — 修复 macOS「复制失败，请手动复制」（08-28）

**根因（外部实锤）**：tauri-plugin-clipboard-manager 在 tokio worker 线程经 arboard 调 NSPasteboard，与 WebKit 主线程粘贴板监控竞态（官方 issue plugins-workspace#3205，macOS 26 高发）→ write_text 失败；原 copyToClipboard 把真实错误吞成统一文案。对照参考：诊断文档 docs/diagnostics-startup-404.html。

**修复三层**：
1. macOS 桌面不再安装插件 shim（tauri-bridge IS_MAC 分支）——点击复制回归 WebKit 原生 navigator.clipboard.writeText（主线程、手势可用）
2. Rust 新命令 clipboard_write_text：run_on_main_thread 派发 arboard 写入（新建实例）；前端 LockClipboard.write 统一入口承接无手势场景（10s 自动清空剪贴板）
3. copyToClipboard：失败降级 execCommand('copy') 兼容通道；Toast 携带真实错误；复制后 UI 装饰异常不再误报失败

**排错插曲**：插入命令时锚点吞掉了 open_url 的 #[tauri::command] 属性致 E0252（双重宏），已修正属性归属并 cargo check 通过。

**验证**：cargo check ✅；vite:build ✅；node --check tauri-bridge ✅；version:check 1.0.12 一致。

**版本**：v1.0.11 → v1.0.12。mac 上验证方式：重构建 .app 覆盖安装后任意条目点复制按钮。

## v1.0.14 — 签名 env 收敛到项目根 .env.local（08-28）

- 用户反馈签名 env 应放项目内；迁移：`.env.local`（gitignore 已补，600 权限）为唯一事实源，wrapper 优先加载，兼容 ~/.tauri 旧位置
- 重要状态：用户 11:26 自行重新生成了带密码加密的密钥对（rsign encrypted secret key），tauri.conf.json 公钥已同步为新密钥对；.env.local 中 PASSWORD 占位已由用户填入真实密码（勿打印）
- 端到端验证：wrapper 链路 tauri signer sign 冒烟成功（.sig 400B）
- 排查工具注意：exec 输入管道会把 === 与 ${{ 序列污染为 =*** 与 ***（node -e 内联脚本与多处补丁字符串中招）；后续含此类序列的内容一律经 python 文件写入 + 字符码断言校验

**版本**：v1.0.10 → v1.0.14（注：v1.0.10 使用指南切 Pages 条目见 v1.0.11 摘要行）

## v1.0.15 — 本地签名改内联注入（08-28）

**复盘**：用户再次报本地 build no private key——v1.0.13/14 包装器只注入 _PATH，而 **tauri build 签名只认内联 TAURI_SIGNING_PRIVATE_KEY**（_PATH 仅 tauri signer 子命令支持；此前冒烟测的是 signer 子命令，验证路径选错导致误判已修复）。

**修复**：wrapper 在 env 加载后统一补齐内联变量（从 .env.local PATH 或兜底私钥文件读取）；私钥首行解 base64 检测 encrypted 态并在缺密码时告警。

**验证（决定性）**：本机实跑 npm run tauri:build → `LockPass.app.tar.gz` + `.sig`（408B）产出，EXIT=0。

**版本**：v1.0.14 → v1.0.15

## v1.0.14 补记 — CI 密码变量改 Secret 引用（11:36）

- release.yml 两处构建 job：TAURI_SIGNING_PRIVATE_KEY_PASSWORD 由空串改为 ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}（密钥已是加密态，空串必失败；硬编码密码则泄露）
- 用户需新增第二个 GitHub Secret（密钥密码），与私钥 Secret 配套
- 写入经验：${{ 字面量会被 exec 管道污染为 ***（磁盘级）；本轮采用「python 构造拼接 + 写后自校验 + 策略回退」闭环写入成功，后续凡含 ${{ 的写入一律走此模式

## v1.0.16 — CI 清单递归修复定版 + 版本日志体系迁移（08-28）

- 用户定版：gen-latest-json.mjs 递归扫描修复 → v1.0.16（📝 待发布）
- 版本日志体系迁移：对齐 fastenerTradeWorkbench 模式，新建 docs/version/（README.md 索引+约定 / RELEASE-v1.0.md 汇总 v1.0.0~v1.0.16），README 内嵌更新日志章节替换为指引；AGENTS.md 更新流程第 4 步同步
- 历史条目原样搬运（146 行，v1.0.15~v1.0.0，v1.0.10 条目此前被用户移除故缺）
- memory/ 目录为 AI 工作日志，不迁入 docs/version
