# 2026-08-30 工作记录

## 点击眼睛按钮显示秘密字段加 Toast 提示 → v1.0.35

**用户需求**：点击眼睛查看密钥时，也需要像右键"临时显示 5 秒"一样弹出提示："已临时显示（5 秒后自动隐藏）"。

**改动**：
1. **`src/components/modals/EntryEditorModal.vue`**：`toggleSecret(k)` 切换为显示时，调用 `window.Utils.showToast('已临时显示（5 秒后自动隐藏）', 'info')`。
2. **`src/composables/useVault.js`**：
   - `toggleDetailPassword()` 切换为显示时同样弹 Toast；
   - 新增 `revealDetailPasswordOnce()`（右键 reveal-once 专用）：强制显示 + 重置计时 + Toast 一次到位，避免"先关再开"的两次翻转。
   - 导出列表补充 `revealDetailPasswordOnce`。
3. **`src/components/entries/DetailPanel.vue`**：右键 `reveal-once` 改调 `revealDetailPasswordOnce()`，避免翻转逻辑 + 重复 Toast。

**验证**：`npm run vite:build` 59 modules 0 error 0 warning（461.81 kB / gzip 126.83 kB）；`npm run version:check` 11 处一致（v1.0.35）。未执行任何 git 操作。

---

## 条目自定义字段扩展（阶段 0） → v1.1.5

**任务来源**：docs/upgrade-design.md 第一章「条目字段扩展」阶段 0（1.1~1.4）。

**改动**：
1. **`src/core/templates.js`（新建）**：`CUSTOM_FIELD_TYPES`（text/pin/email/phone/otp/url/notes 枚举 + labelKey/icon/placeholder）与 `FIELD_TEMPLATES`（银行/邮箱/WiFi/服务器/社交/自定义 6 套模板），`createCustomField(label, type)` 工厂，挂载 `window.CustomFieldTemplates`；`src/main.js` 注册导入。
2. **`src/composables/useVault.js`**：
   - 解锁迁移 `migrateEntry` 补 `customFields: []` 默认；
   - `getFilteredEntries` 搜索命中 `customFields[].value`（含 label 命中）；
   - `saveEntry` 归一化：非数组置空、字段清洗（非法 type 回落 text、label 截断 50、value 转字符串）。
3. **存储与迁移（§1.3）**：`ExportModal` 导出追加 `schemaVersion: 2`；`import-bridge.js` mergeEntries 补默认；`ImportModal.vue` 加密/明文/CSV 三分支补 `customFields` 默认；`QrImportModal.vue` newEntry 补 `customFields: []`。
4. **详情页（§1.4）**：`CustomFieldRow.vue`（新建，敏感默认掩码 + 眼睛揭示 + 复制）；`DetailPanel.vue` 自定义字段分组（visibleCustomFields computed + customReveal map + toggleCustomReveal）、右键菜单 custom-field case（复制/揭示）+ action 处理补齐。
5. **编辑弹窗（§1.4）**：`EntryEditorModal.vue` 自定义字段区块——模板一键应用（applyTemplate）、手动添加（addCustomField）、删除/上下移动（moveCustomField）、敏感开关 + 编辑态掩码（toggleCfReveal）、onSave/snapshotForm/persistDraft 深拷贝、onMounted 草稿恢复。
6. **i18n**：zh/en 词典新增 `editor.custom.*` 与 `detail.custom.*`（title/add/template/type/label/placeholder/sensitive/empty/moveUp/moveDown/remove/show/hide/ctxCopy/ctxReveal 等）。
7. **样式**：`src/styles/editor.css` 追加 `.cf-tpl-row/.cf-add-row/.cf-list/.cf-row*`；详情页复用 entries.css 既有 detail-field/masked 类。

**验证**：`npm run vite:build` 64 modules 0 error 0 warning；`npm run version:set 1.1.5` 全量同步（package.json/tauri.conf.json/extension/Cargo.toml/AGENTS.md/spec.md）；`npm run version:check` 11 处 v1.1.5 一致；版本日志 RELEASE-v1.1.md 与 docs/version/README.md 索引已更新。未执行任何 git 操作、未 tauri build。
