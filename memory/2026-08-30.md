# 2026-08-30 工作记录

## 点击眼睛按钮显示秘密字段加 Toast 提示 → v1.0.35

**用户需求**：点击眼睛查看密钥时，也需要像右键"临时显示 5 秒"一样弹出提示："已临时显示（5 秒后自动隐藏）"。

**改动**：
1. **`src/components/modals/EntryEditorModal.vue`**：`toggleSecret(k)` 切换为显示时，调用 `window.Utils.showToast('已临时显示（5 秒后自动隐藏）', 'info')`。
2. **`src/composables/useVault.js`**：
   - `toggleDetailPassword()` 切换为显示时同样弹 Toast；
   - 新增 `revealDetailPasswordOnce()`（右键 reveal-once 专用）：强制显示 + 重置计时 + Toast 一次到位，避免"先关再开"的两次翻转。
   - 导出列表补充 `revealDetailPasswordOnce`。
3. **`src/components/entries/DetailPanel.vue`**：右键 `reveal-once` 改调 `revealDetailPasswordOnce()`，避免翻转逻辑 + 重复 Toast。

**验证**：`npm run vite:build` 59 modules 0 error 0 warning（461.81 kB / gzip 126.83 kB）；`npm run version:check` 11 处一致（v1.0.35）。未执行任何 git 操作。
