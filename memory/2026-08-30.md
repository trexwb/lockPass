# 2026-08-30 工作记录

## 点击眼睛按钮显示秘密字段加 Toast 提示 → v1.0.35

**用户需求**：点击眼睛查看密钥时，也需要像右键"临时显示 5 秒"一样弹出提示："已临时显示（5 秒后自动隐藏）"。

**改动**：
1. **`src/components/modals/EntryEditorModal.vue`**：`toggleSecret(k)` 切换为显示时，调用 `window.Utils.showToast('已临时显示（5 秒后自动隐藏）', 'info')`。
2. **`src/composables/useVault.js`**：
   - `toggleDetailPassword()` 切换为显示时同样弹 Toast；
   - 新增 `revealDetailPasswordOnce()`（右键 reveal-once 专用）：强制显示 + 重置计时 + Toast 一次到位，避免"先关再开"的两次翻转。
   - 导出列表补充 `revealDetailPasswordOnce`。
3. **`src/components/entries/DetailPanel.vue`**：右键 `reveal-once` 改调 `revealDetailPasswordOnce()`，避免翻转逻辑 + 重复 Toast。

**验证**：`npm run vite:build` 59 modules 0 error 0 warning（461.81 kB / gzip 126.83 kB）；`npm run version:check` 11 处一致（v1.0.35）。未执行任何 git 操作。

---

## 条目卡片左滑显示操作（移动端 swipe-to-reveal）→ v1.1.10

**用户需求**：列表中的操作按钮改成左滑手势显示，控制标签不换行、释放卡片水平空间。

**交互模型**（iOS / Gmail 风格）：
- 双层卡片结构：外层 `.entry-card-wrap` + 背景操作层 `.card-actions-backdrop`（3 按钮）+ 前景滑动层 `.entry-card`。
- 手指左滑前景卡片，跟随位移露出右侧操作层；松手：超过 35% 阈值吸附打开，否则回弹关闭。
- 互斥：同一时刻仅允许 1 张卡片打开；点击空白 / 垂直滚动 / 切卡片自动收起。
- 水平橡皮筋手感（120% 过载），快速关闭（右滑已打开卡片无需阈值）。
- 触屏设备（≤1024px 或 `pointer: coarse`）：隐藏内联 `.entry-actions`，左滑为唯一入口；避免按钮挤占标签空间导致换行。
- 桌面端（>1024px 且可 hover）：隐藏背景层，保留 hover 显示内联按钮。
- `prefers-reduced-motion`：关闭 transition / animation / hover 位移。
- 长按菜单兼容：swipe 一旦判定水平手势，取消长按计时，避免误触发右键菜单。

**改动文件**：
1. **`src/composables/useSwipeActions.js`（新建 L1–176）**：通用左滑手势 composable。
   - 状态：`offsetMap / draggingMap / _openId`（按 entryId 索引，互斥）。
   - 方向锁：`|dx| > |dy| + 6` 判定水平锁定；垂直优先时顺带关闭已打开卡片。
   - 边界钳制：`target ∈ [-1.2×W, 0]`，橡皮筋 sqrt 软限。
   - API：`getCardStyle / cardTouchHandlers / closeAll / openCard / closeCard`。
2. **`src/components/AppShell.vue`**：
   - 引入 `useSwipeActions`（L11、L47–L50），设置 `ACTIONS_WIDTH = 156px`，定义 `_isMobileSwipe()`（≤1024px）。
   - 条目卡片结构重构（L539–L665）：外层 `.entry-card-wrap` → 背景 `.card-actions-backdrop`（收藏/复制/删除 或 恢复/复制/彻底删除）→ 前景 `.entry-card`。
   - 新增触摸包装函数（L389–L418）：`onEntryTouchStart / onEntryTouchMove / onEntryTouchEnd / onEntryTouchCancel`，协调长按菜单与 swipe 手势。
   - 列表容器绑定 `@click.capture="onListClickForSwipe"` 互斥关闭。
3. **`src/styles/entries.css`**：
   - 新增 `.entry-card-wrap / .card-actions-backdrop / .backdrop-inner / .swipe-action-btn` 样式（L22–L130）：双层容器、按钮语义色（收藏=橙/复制=绿/删除=红/恢复=蓝）、拖拽态 shadow 提升。
   - `.entry-card` 移除 `margin-bottom`（由 wrap 控制），加 `z-index: 2`，`touch-action: pan-y manipulation`。
   - `@media (max-width: 1024px)`：隐藏 `.entry-actions`，启用背景层（L824–L830）。
   - `@media (hover: none) and (pointer: coarse)`：触屏大屏同样启用 swipe + 隐藏内联按钮（L986–L993）。
   - `@media (max-width: 480px)`：手机端 swipe 按钮 min-height 44px、20px icon、间距收紧。
   - `@media (prefers-reduced-motion: reduce)`：全站 swipe/card/scan 动效降级。

**验证**：`npm run vite:build` 69 modules 0 error 0 warning（633.11 kB / gzip 176.02 kB）；`npm run version:check` 11 处一致（v1.1.10）。未执行任何 git 操作。

---

## 修复：登录时卡片操作按钮「一闪而过」（FOUC） → v1.1.11

**用户反馈**：解锁保险箱后，卡片右侧的收藏/复制/删除按钮会先显示（闪一下）然后才消失。

**根因**（双重 FOUC）：
1. 基础样式 `.entry-actions { display:flex; opacity:0; transition:... }` → CSS 解析前的瞬间，浏览器默认将 `opacity:1`（无 CSS）渲染为可见按钮；CSS 解析完毕后 `opacity:0` 生效 → **视觉上"闪一下就没了"**。
2. 移动端/触屏的隐藏策略（`@media (max-width:1024px)` 或 `@media (hover:none)` 中写 `display:none !important`）依赖媒体查询加载，而 file:// 协议无缓存时媒体查询延迟生效 → iPad/触屏设备首帧显示出大号（36/44px）按钮，随后才消失。
3. 截图（iPad 1668×2224 分辨率）证实：内联按钮挤占卡片右侧空间，导致标签与日期文本被迫换行、视觉重叠。

**修复（移动端优先 + 防 FOUC）**：
`src/styles/entries.css` 统一重构 `.entry-actions` 的显示策略：

1. **基础默认 `display:none`**：卡片内联操作按钮在所有视口/指针设备下，DOM 解析到的第一状态就是不渲染。无论 CSS 是否加载、媒体查询是否命中，**永远不会"闪一下"**。
2. **仅桌面端启用内联按钮**：`@media (min-width: 1025px) and (hover:hover) and (pointer:fine)` 才将 `.entry-actions` 置为 `display:flex`，并添加以下效果：
   - `opacity:0 + visibility:hidden + translateX(6px) + pointer-events:none` → 初始不可见/不可点
   - `:hover / :focus-within` → `opacity:1 + visibility:visible + translateX(0) + pointer-events:auto` → 渐显 + 6px 滑入
   - `.entry-card.recycled`（回收站）→ 桌面端始终显示（便于恢复/彻底删除）
3. **删除冗余规则**：
   - `@media (max-width:1024px)` 删除 `.entry-actions { display:none !important }`（基础样式已 display:none）
   - `@media (hover:none)` 删除 `.entry-actions { display:none !important }`（同上）
   - `@media (max-width:480px)` 删除 `.entry-actions button, .copy-btn, .delete-btn, .star-btn, .restore-btn` 尺寸 + `.entry-actions { gap:6px }`（内联按钮已不显示，仅保留 `.swipe-action-btn` min-height 44px 设置）
4. **移动端操作入口由左滑背景操作层提供**（v1.1.10 引入的 swipe-to-reveal），桌面端由 hover 提供，两套入口完全解耦，桌面端不会加载移动端的按钮尺寸。

**验证**：`npm run vite:build` 69 modules 0 error 0 warning（633.30 kB / gzip 176.07 kB）；`npm run version:check` 11 处一致（v1.1.11）。未执行任何 git 操作。

---

## 条目自定义字段扩展（阶段 0） → v1.1.5

**任务来源**：docs/upgrade-design.md 第一章「条目字段扩展」阶段 0（1.1~1.4）。

**改动**：
1. **`src/core/templates.js`（新建）**：`CUSTOM_FIELD_TYPES`（text/pin/email/phone/otp/url/notes 枚举 + labelKey/icon/placeholder）与 `FIELD_TEMPLATES`（银行/邮箱/WiFi/服务器/社交/自定义 6 套模板），`createCustomField(label, type)` 工厂，挂载 `window.CustomFieldTemplates`；`src/main.js` 注册导入。
2. **`src/composables/useVault.js`**：
   - 解锁迁移 `migrateEntry` 补 `customFields: []` 默认；
   - `getFilteredEntries` 搜索命中 `customFields[].value`（含 label 命中）；
   - `saveEntry` 归一化：非数组置空、字段清洗（非法 type 回落 text、label 截断 50、value 转字符串）。
3. **存储与迁移（§1.3）**：`ExportModal` 导出追加 `schemaVersion: 2`；`import-bridge.js` mergeEntries 补默认；`ImportModal.vue` 加密/明文/CSV 三分支补 `customFields` 默认；`QrImportModal.vue` newEntry 补 `customFields: []`。
4. **详情页（§1.4）**：`CustomFieldRow.vue`（新建，敏感默认掩码 + 眼睛揭示 + 复制）；`DetailPanel.vue` 自定义字段分组（visibleCustomFields computed + customReveal map + toggleCustomReveal）、右键菜单 custom-field case（复制/揭示）+ action 处理补齐。
5. **编辑弹窗（§1.4）**：`EntryEditorModal.vue` 自定义字段区块——模板一键应用（applyTemplate）、手动添加（addCustomField）、删除/上下移动（moveCustomField）、敏感开关 + 编辑态掩码（toggleCfReveal）、onSave/snapshotForm/persistDraft 深拷贝、onMounted 草稿恢复。
6. **i18n**：zh/en 词典新增 `editor.custom.*` 与 `detail.custom.*`（title/add/template/type/label/placeholder/sensitive/empty/moveUp/moveDown/remove/show/hide/ctxCopy/ctxReveal 等）。
7. **样式**：`src/styles/editor.css` 追加 `.cf-tpl-row/.cf-add-row/.cf-list/.cf-row*`；详情页复用 entries.css 既有 detail-field/masked 类。

**验证**：`npm run vite:build` 64 modules 0 error 0 warning；`npm run version:set 1.1.5` 全量同步（package.json/tauri.conf.json/extension/Cargo.toml/AGENTS.md/spec.md）；`npm run version:check` 11 处 v1.1.5 一致；版本日志 RELEASE-v1.1.md 与 docs/version/README.md 索引已更新。未执行任何 git 操作、未 tauri build。

---

## 扩展 Firefox 兼容层（docs/upgrade-design.md 第三章） → 版本号未变更

**任务来源**：docs/upgrade-design.md 第三章「Firefox 兼容层」（3.1 manifest 适配 / 3.2 代码兼容清单 / 3.3 验证）。

**代码兼容性核查结论（无需改写扩展 JS）**：
- background.js / content.js / popup.js / lockpass-bridge.js 全部为 promise 风格 `chrome.*`（await/.catch），无回调式 API，Firefox 115+ 对 `chrome.action/tabs/runtime/storage` promise 语义全支持；
- `chrome.tabs.sendMessage` 已显式传 frameId（Firefox 不指定 frameId 仅发顶层，显式传更一致）；`chrome.runtime.onMessage` return true 异步响应 Firefox 支持；
- 故「同一份扩展代码」不改动，仅派生 Firefox 变体 manifest。

**改动**：
1. **`scripts/build-extension.mjs`（新建）**：双版本打包脚本——
   - Chrome 版：拷贝 extension/ → `dist/extension/chrome/`，zip 为 `dist/lockpass-extension-v<版本>.zip`（保持既有发布链接不变）；
   - Firefox 版：拷贝 extension/ → `dist/extension/firefox/`，派生变体 manifest（注入 `browser_specific_settings.gecko`：id=`lockpass-extension@lockpass.local`、strict_min_version=115.0；background 由 `service_worker` 改为 Firefox 的 `scripts` 声明，规避 SW 事件驱动休眠导致 setInterval 15s 轮询停摆），web-ext 打包为 `dist/lockpass-extension-v<版本>-firefox.xpi`（web-ext 缺失时自动回退系统 zip）；
   - 内置 Firefox manifest 校验（gecko id / min_version / background.scripts / manifest_version=3）与产物存在性校验。
2. **`.github/workflows/pages.yml`**：扩展打包步骤改为调用 `node scripts/build-extension.mjs`，同时产出 Chrome zip + Firefox xpi 并校验；注释更新。
3. **`README.md`**：新增「方式三：Firefox 安装（Firefox 115+，MV3）」说明（about:debugging 临时加载 .xpi）。

**验证**：
- `node --check` 扩展 4 个 JS + 脚本全部通过；
- `node scripts/build-extension.mjs` 幂等通过：Chrome zip（47K）+ Firefox xpi（46K，web-ext 路径）生成，Firefox manifest 校验通过；
- 本机未安装 Firefox，about:debugging 手动加载与填充回归（单/多字段/iframe/shadow DOM）留待用户按 README 方式三执行。

**版本号说明**：Firefox 兼容属功能增强（MINOR 需用户决定），且扩展 manifest.json / package.json / tauri.conf.json 均未改动（Firefox 变体为构建期派生），故本次未升版本号。未执行任何 git 操作。

---

## 移动端抽屉滑动关闭手势 → v1.1.8

**用户需求**：手机端密码详情面板是从右侧滑出的抽屉，应支持手指右滑关闭；侧边栏同理支持左滑关闭。

**设计参考**：trae-remote-official:web-app-development:uicraft skill 的 interaction-design.md（"Making Gestures Discoverable" 原则）与 motion-design.md（300-500ms drawer duration、transform-only 动画）。

**改动**：

1. **`src/composables/useSwipeClose.js`（新建）**：通用水平滑动关闭 composable。
   - `touchstart` 记录起点 → `touchmove` 判断水平/垂直主导（水平位移 > 垂直+6px 死区锁定为"拖拽"态）→ `touchend` 超阈值（80px）或快速滑动（<300ms 且 >40px）触发 `onClose`。
   - `dragOffset` ref 驱动面板跟随手指位移；`isDragging` 标记拖拽态。
   - `isEnabled` 回调：返回 false 时禁用手势（桌面端非抽屉模式不启用）。
   - 垂直滚动优先：未锁定为水平拖拽前不干预垂直滚动（`touch-action: pan-y`）。

2. **`src/components/entries/DetailPanel.vue`**：接入右滑关闭（panel 在右侧 → `direction: 'right'`）。
   - `isEnabled: () => matchMedia('(max-width: 1024px)').matches && !!entry.value`（仅移动端抽屉模式）。
   - `panelStyle` computed：拖拽时内联 `transform: translateX(${dragOffset}px); transition: none`。
   - 模板：`@touchstart.passive` / `@touchmove.passive` / `@touchend.passive` / `@touchcancel.passive` 绑定。
   - 新增 `.swipe-hint` 元素（左边缘细条 + 脉冲动画，暗示可滑动）。

3. **`src/components/layout/SidebarNav.vue`**：接入左滑关闭（panel 在左侧 → `direction: 'left'`）。
   - `isEnabled: () => matchMedia('(max-width: 1024px)').matches && vaultState.sidebarOpen`。
   - 同样 `sidebarStyle` computed + 模板 touch 事件绑定 + `.swipe-hint` 元素（右边缘）。

4. **CSS（entries.css / layout.css）**：
   - `#detail-panel` / `#sidebar` 加 `touch-action: pan-y`（允许垂直滚动、捕获水平手势）。
   - `.swiping` 类：拖拽时加深 `box-shadow` + `user-select: none`（防止拖拽中误选文本）。
   - `.swipe-hint`：3px × 36px 圆角细条，`--text-tertiary` 色，`swipe-hint-pulse` 脉冲动画 2.4s 循环；拖拽中隐藏。
   - 仅 `@media (max-width: 1024px)` 可见（桌面端不显示）。

**设计原则遵循**：
- "Duplicate the path"：面板已有关闭按钮 (X)，滑动只是快捷方式，不是唯一路径。
- "Making Gestures Discoverable"：`.swipe-hint` 脉冲细条作为视觉暗示。
- "Animate Transform only"：拖拽跟随仅操作 `transform`，松手回弹走 CSS `transition: transform`。
- `prefers-reduced-motion`：base.css 已全局将 `transition-duration` 降为 0.01ms；直接操作（拖拽跟随）不受影响。

**验证**：`npm run vite:build` 67 modules 0 error 0 warning（618.19 kB / gzip 172.66 kB）；`npm run version:check` 11 处一致（v1.1.8）。未执行任何 git 操作。

---

## 移动端删除确认 + 触控交互优化 → v1.1.9

**用户需求**：手机端的交互动作需要调整，比如删除密码应该有确认动作。

### 1. softDelete 确认弹窗

**问题**：`softDelete`（移入回收站）有 4 个调用入口全部直接执行无确认——卡片删除按钮、卡片长按菜单、详情页底部按钮、详情页右键菜单。移动端触控极易误触。

**改动**：
1. **`src/composables/useVault.js`** `softDelete(id)` 函数（856 行）：在 `entry.deletedAt` 赋值前插入 `await window.Utils.confirm({ title, message, confirmText, danger: true })`，用户取消则 return。4 个入口点全部统一走此函数，全覆盖。
2. **`src/i18n/zh.json` / `src/i18n/en.json`**：新增 `vault.confirm.softDelete.title` / `.msg` / `.ok` 三组 key（中英双语）。

### 2. 移动端触控交互优化

**改动**：

1. **触控目标尺寸（`src/styles/entries.css`）**：
   - 新增 `@media (max-width: 480px)` 媒体查询：卡片操作按钮（copy/delete/star/restore）从 28px → **44px**（Apple HIG 触控下限）；
   - `.entry-actions` gap 从 4px → 6px（间距匹配更大按钮）。

2. **长按视觉反馈（`src/styles/entries.css` + `src/components/AppShell.vue`）**：
   - CSS 新增 `.entry-card.long-pressing` 类：`scale(0.97)` + 强调色边框 + `box-shadow` 光环；
   - `.entry-card` 加 `touch-action: manipulation`（禁用双击缩放，保证长按不被中断）+ `-webkit-tap-highlight-color: transparent`；
   - JS `onCardTouchStart`：触摸开始时加 `long-pressing` class + 轻振动反馈（10ms）；长按触发时清除 class + 强振动（30ms）；`cancelLongPress` 清除 class。

3. **详情页底部按钮（`src/styles/layout.css`）**：
   - `@media (max-width: 480px)` 中 `.detail-footer .btn` 加 `min-height: 44px`。

**验证**：babel 语法解析通过（useVault.js / AppShell.vue / DetailPanel.vue）；CSS 花括号平衡（entries.css 185/185、layout.css 201/201）；i18n JSON 校验通过；`npm run version:set 1.1.9` + `npm run version:check` 11 处一致。未执行任何 git 操作。
