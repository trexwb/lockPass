# 2026-08-24 变更记录

## v1.0.0 → v1.0.1

### 修复：桌面构建白屏（C1）

- **文件**：`src-tauri/tauri.conf.json`
- **问题**：`build.beforeBuildCommand` 为 `"vite build"`（单文件内联模式），
  产物 `index.html` 是内联 `<script type="module">`，被严格 CSP
  `script-src 'self'`（无 `unsafe-inline`）拦截，导致桌面 WebView 白屏。
- **修复**：`beforeBuildCommand` 改为 `"vite build --mode tauri"`，
  走 tauri 模式构建（关闭 viteSingleFile，产出外部 JS/CSS 资源），与 CSP 匹配。

### 验证

- `vite build --mode tauri` 构建通过（42 modules）。
- `dist/index.html` 为外部资源引用（`./assets/index-*.js` / `.css`），无内联脚本。
- `dist/sw.js` 版本注入正常：`CACHE_NAME = 'lockpass-v1.0.0'`（构建时注入）。
- `version:check` 10 处版本号一致（v1.0.1）。

### 备注

- 未执行任何 Git 提交（AGENTS.md 硬约束）。
*（内容由AI生成，仅供参考）*
