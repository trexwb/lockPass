# 2026-08-24 — Vue3 迁移检查与修复

## 工作内容

对 `refactor/vue` 分支（Vue3 + Vite 迁移）做了全面检查，修复致命 bug，版本升级 **v1.0.13 → v1.0.1**（PATCH，符合用户硬性约束）。

## 发现并修复的问题

1. **🔴 致命：创建保险库后卡在认证界面** — `useVault.js` `handleUnlock()` 创建分支漏设 `vaultState.isUnlocked = true`（原生 app.js 683-685 行有）。已修复：接收 `createVault` 返回的 key、设置 `cryptoKey` 与 `isUnlocked`。
2. **🔴 致命：toast 全局崩溃** — Vue 版 App.vue 漏掉 `<div id="toast-container">` 挂载点（原生在 template.js），`showToast` 直接 `getElementById(...).appendChild` 抛 TypeError，导致保存/复制/退出等所有 toast 调用崩溃。已修复。

## 验证结果

- 双产物（dist HTTP 模式 + dist-single file:// 双击模式）全流程 e2e 通过：创建 → 主界面 → 添加条目（标题+密码）→ 列表显示 → 刷新 → 解锁 → 数据持久化，无 JS 错误。
- 验证脚本：`~/.openclaw-autoclaw/agents/agent-gjb2ig/workspace/.openclaw/tmp/e2e-lockpass.cjs`（HTTP）、`e2e-single.cjs`（file://）

## 遗留问题（未修，需用户决策）

1. **7 个功能模态框是占位符**：设置 / 批量导入 / 导出 / 二维码添加 / 二维码分享 / 修改主密码 / 标签管理（ModalHost.vue PENDING_MODAL_NAMES 全部显示"后续迁移轮次提供"）——原生 settings.js / import-export.js / qr-sync.js 逻辑未迁移
2. 安全上下文检查（isSecureContext 友好提示）未迁移，http 非 localhost 下 Web Crypto 报错无提示
3. 旧 storage 清理（lp_remember_pw / lp_bind_prompted）未迁移
4. 死代码残留：src/js/ 下 app.js、main.js、editor.js、entries.js、import-export.js、qr-sync.js、settings.js、shortcuts.js、template.js、ui.js 不再被引用（仅 particles.js 在用）
5. bump-version.mjs 已更新覆盖新路径（core/version.js、vite.config.js、public/sw.js），但注意 src/js/app.js、src/sw.js 仍是死代码

## 版本号状态

v1.0.1 已同步：package.json / package-lock.json / tauri.conf.json / Cargo.toml / AGENTS.md / SPEC.md / src/core/version.js / vite.config.js(__APP_VERSION__) / src/public/sw.js(CACHE_NAME) / src/js/app.js / src/sw.js。**未 git 提交**（用户自行决定）。

## 第二轮：设置域模块迁移（v1.0.14 → v1.0.15）

按用户指示「按模块逐个做」，完成设置域 3 个模块迁移，全部通过双产物 e2e（HTTP + file:// 各 14 项断言）：

1. **设置模态框**（SettingsModal.vue，复刻 settings.js）：安全（自动锁定/剪贴板清除，localStorage 持久化）、本地文件同步（绑定/重新绑定目录）、标签管理入口、数据说明（4 卡片 + 存储架构）、数据管理（导入/扫码导入/导出入口 + 修改主密码 + 销毁保险箱双重确认）、快捷键说明表（27 项）、关于版本号
2. **修改主密码**（ChangePwModal.vue）：验证旧密码 → 新盐+新密钥 → 重新加密 → 更新 cryptoKey → syncNow
3. **标签管理**（TagsModal.vue）：列表（默认排前/计数/颜色样本）、新增/编辑（名称/颜色/图标选择器）、重命名同步条目标签、删除（confirm 确认）

### 过程中发现并修复的 bug
- **ModalBase z-index 硬编码 1000**：盖住了 confirm 弹窗（原生设计 --z-confirm:400 > --z-modal:200），模态框内触发删除/销毁确认无法点击。改回 var(--z-modal)
- **handleUnlock 创建分支漏 `initialized = true`**：锁定后 AuthView 误判为创建模式（显示"创建密码保险箱"），报"两次输入的密码不一致"。已补
- **main.js 漏 import core/version.js**：window.LockPassVersion 不存在，设置页版本号空白。已补

### 遗留（下一轮）
- 批量导入（import-export.js 665 行）
- 导出（JSON/CSV）
- 二维码添加/分享（qr-sync.js 712 行，依赖 jsQR/qrcode vendor）

版本 v1.0.15 已同步 10 处（bump-version.mjs 自动，含 core/version.js / vite.config.js / public/sw.js）。未 git 提交。

## 第三轮：构建体系统一单文件产物（v1.0.15 → v1.0.16）

用户要求：让 vue3 打包的 dist 直接双击浏览器可运行，去掉单独 dist-single。

### 改动
- **vite.config.js**：移除 mode=single 分支，viteSingleFile 恒启用，outDir 恒为 ../dist（不再产出 dist-single）
- **package.json**：build:single 改为 vite build 兼容别名，build:all = npm run build
- 删除旧 dist-single 目录

### 验证（全通过）
- file:// 双击 dist/index.html：认证界面正常渲染，零报错（此前标准构建被 CORS 拦截白屏）
- HTTP 部署：核心流程 7/7 + 设置域 14/14，零 JS 错误
- PWA：/sw.js 正常注册（SW 文件仍在 dist，HTTP 部署下更新机制不受影响）
- 单文件 246KB（gzip 75KB），外链 JS/CSS 为 0，vendor（jsQR/qrcode）按需动态加载保持外链

### 权衡说明（已确认可接受）
- 失去按 hash 拆分的资源缓存粒度（整文件缓存），对单页小应用无感
- file:// 下 SW 仍被浏览器禁用（浏览器限制，无法绕过），PWA 能力仅 HTTP 模式
