# 2026-08-24 — Vue3 迁移检查与修复

## 工作内容

对 `refactor/vue` 分支（Vue3 + Vite 迁移）做了全面检查，修复致命 bug，版本升级 **v1.0.13 → v1.0.1**（PATCH，符合用户硬性约束）。

## 发现并修复的问题

1. **🔴 致命：创建保险库后卡在认证界面** — `useVault.js` `handleUnlock()` 创建分支漏设 `vaultState.isUnlocked = true`（原生 app.js 683-685 行有）。已修复：接收 `createVault` 返回的 key、设置 `cryptoKey` 与 `isUnlocked`。
2. **🔴 致命：toast 全局崩溃** — Vue 版 App.vue 漏掉 `<div id="toast-container">` 挂载点（原生在 template.js），`showToast` 直接 `getElementById(...).appendChild` 抛 TypeError，导致保存/复制/退出等所有 toast 调用崩溃。已修复。

## 验证结果

- 双产物（dist HTTP 模式 + dist-single file:// 双击模式）全流程 e2e 通过：创建 → 主界面 → 添加条目（标题+密码）→ 列表显示 → 刷新 → 解锁 → 数据持久化，无 JS 错误。
- 验证脚本：`~/.openclaw-autoclaw/agents/agent-gjb2ig/workspace/.openclaw/tmp/e2e-lockpass.cjs`（HTTP）、`e2e-single.cjs`（file://）

## 遗留问题（未修，需用户决策）

1. **7 个功能模态框是占位符**：设置 / 批量导入 / 导出 / 二维码添加 / 二维码分享 / 修改主密码 / 标签管理（ModalHost.vue PENDING_MODAL_NAMES 全部显示"后续迁移轮次提供"）——原生 settings.js / import-export.js / qr-sync.js 逻辑未迁移
2. 安全上下文检查（isSecureContext 友好提示）未迁移，http 非 localhost 下 Web Crypto 报错无提示
3. 旧 storage 清理（lp_remember_pw / lp_bind_prompted）未迁移
4. 死代码残留：src/js/ 下 app.js、main.js、editor.js、entries.js、import-export.js、qr-sync.js、settings.js、shortcuts.js、template.js、ui.js 不再被引用（仅 particles.js 在用）
5. bump-version.mjs 已更新覆盖新路径（core/version.js、vite.config.js、public/sw.js），但注意 src/js/app.js、src/sw.js 仍是死代码

## 版本号状态

v1.0.1 已同步：package.json / package-lock.json / tauri.conf.json / Cargo.toml / AGENTS.md / SPEC.md / src/core/version.js / vite.config.js(__APP_VERSION__) / src/public/sw.js(CACHE_NAME) / src/js/app.js / src/sw.js。**未 git 提交**（用户自行决定）。
