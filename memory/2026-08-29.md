# 2026-08-29

## 功能设计多维度深度评审（纯文档，未改代码，不升版本）

- 产出报告：`docs/audit-2026-08-29-功能设计多维度深度评审.md`
- 审读范围：App.vue / AppShell / AuthView / DetailPanel / EntryEditorModal / SidebarNav / HeaderBar / ModalBase / ModalHost / QrShareModal / SettingsModal(部分) / useVault / useShortcuts / base.css / docs/spec.md
- 结论：无 P0；2 个 P1：
  1. `lockVault()/logout()` 未清 `vaultState.history`（历史快照含修改前明文密码/私钥）→ 锁定后明文滞留内存。
  2. EntryEditor 草稿明文密码写 sessionStorage（`lockpass_draft_*`），锁定/登出不清理。
- 其余重点：P2 级含编辑已删条目静默丢数据（saveEntry idx===-1）、右键菜单 spec 承诺未实现、spec 字段模型未回写（host→url）+ 章节编号重复、主界面键盘可达性缺失（SidebarNav/entry-card 为 div，设置按钮 tabindex=-1）、编辑器 copyText 绕过剪贴板自动清除、cmd-field 硬编码 rgba(255,255,255,.15) 浅色主题不可见、AI 类型死字段 organization、创建模式缺「主密码无法找回」警示。
- 修复建议已按三批次排序，第一批为安全收口（+1 行清 history、锁定清草稿、复制统一链路）。
- 用户尚未确认是否执行修复，按用户习惯先出报告待讨论。

## 审计问题全量修复 → v1.0.22（用户拍板：全部修完 + 实现右键菜单 + DetailPanel 重构；版本号由用户定为 PATCH 级 v1.0.22，覆盖此前 v1.1.0 方案）

**版本**：v1.0.21 → v1.0.22（`npm run version:set 1.0.22`，11 处版本号校验一致；虽含右键菜单等功能新增，用户明确指定只升 PATCH）

**修改文件清单**：
- `src/composables/useVault.js`：P1-1 lockVault/logout 补 `history={}`；P1-2 新增 `clearEditorDrafts()` 清 `lockpass_draft_*`；P2-1 saveEntry 编辑已删条目报错并保持编辑器打开；P2-6 selectEntry/closeDetail 动画改 `vaultState.detailAnim`（'collapse'/'reopen'）响应式驱动
- `src/components/modals/EntryEditorModal.vue`：P2-9 copyText 复用 `copyToClipboard`（30 秒自动清除）；P2-11 删除 organization 死字段；P3-13 备注 maxlength=256；图标按钮补 aria-label
- `src/components/AppShell.vue`：P2-2 右键菜单（ctxMenu reactive + Teleport + mousedown/scroll/resize 关闭监听）；P2-4 卡片 role=button/tabindex=0/data-id/Enter+Space；P2-5 aria-label；P3-1 contentTitle 改 computed；P3-8 类型 title 中文；P3-10 空态走 openEntryModal()
- `src/components/layout/SidebarNav.vue`：P2-4 nav-item 全部 role=button+tabindex+onNavKey
- `src/components/layout/HeaderBar.vue`：设置按钮去掉 tabindex="-1"
- `src/components/auth/AuthView.vue`：P2-10 创建模式「主密码无法找回」警示；P3-7 防自动填充改清 ref
- `src/components/entries/FieldRow.vue`（新建）/ `SecretFieldRow.vue`（新建）：P2-7 DetailPanel 六类型字段块抽取的复用行组件
- `src/components/entries/DetailPanel.vue`：全量重构为组件化模板（476→~330 行）；P3-1 ssh/mysql 命令、renderNotes 改 computed；open 类改 panelOpen/panelAnimating 绑定
- `src/composables/useShortcuts.js`：P2-12 ↑/↓ 列表导航（拦截式分发+scrollIntoView）；P3-3 isMac 改 userAgentData+platform 兜底；速查表补 list-nav 条目
- `src/core/utils.js`：P3-6 新增 `safeTagColor()` 十六进制白名单，renderTagChip/getCategoryIcon 注入前校验
- `src/styles/`：base.css P2-8 cmd-field 改 `var(--border-dim)` + 新增 `--warning-border` 令牌（深浅两主题）；entries.css 追加 .ctx-menu/.ctx-item；layout.css 追加 .lock-warning
- `docs/spec.md`：P2-3 字段表对齐 url/password、章节重编号（3.18 设置/3.19 自动更新/3.20 快捷键）、快捷键表对齐实际 28 键、§3.8 右键菜单、§5.2 锁定清理范围补 history/草稿
- `AGENTS.md`：目录结构补 FieldRow/SecretFieldRow
- `CHANGELOG.md`：补 v1.0.22 完整条目

**验证**：`npm run build` 全链路通过（vite + remove-dist-sw + tauri release 打包 + dmg）。未执行任何 git 操作（遵守硬性约束）。

**遗留（未修，列入观察）**：P3-2 Esc 双重监听、P3-4 SVG 双体系、P3-5 虚拟滚动、P3-9 解锁退避、P3-11 内联 toast 样式、P3-12 i18n；以及 DetailPanel app 类型「公钥」行复制走 copyPassword（app 语义复制 appId）的既有行为——纯重构未改，待用户决策是否算 bug。

## 多维度复审（响应式专项）+ v1.0.23（用户要求：客户端自适应样式检查优化 + 手机比例兼容）

**产出报告**：`docs/audit-2026-08-29-响应式与体验复审.md`（无 P0；R1–R4 修复项 + R5–R9 低成本改进，全部落地）

**版本**：v1.0.22 → v1.0.23（PATCH：样式令牌修复 + 移动端适配，用户此前已确认该量级走 PATCH）

**修改文件清单**：
- `src/styles/base.css`：新增 `--text-secondary` 令牌（深 #b6c2cf / 浅 #444c56），修复 5 处未定义变量引用
- `src/styles/entries.css`：ctx-item 13px→0.93rem；coarse 指针下 entry-card user-select:none + ctx-item ≥44px + :active 反馈 + 菜单 180px；.entry-date 去重死代码
- `src/styles/layout.css`：.btn-dropdown-menu 硬编码白色改 var(--border)/var(--border-dim)/var(--shadow-lg)
- `src/components/AppShell.vue`：长按 500ms 呼出右键菜单（touchstart 计时/touchmove·end·cancel 取消/unmount 清理）；empty-active 改模板 :class 绑定（去 watch+getElementById 反模式）；tabbar 标签按钮补 aria-expanded
- `src/components/layout/HeaderBar.vue`：汉堡按钮补 aria-expanded
- `src/components/entries/DetailPanel.vue`：tagStyle() 的 --chip-color 过 safeTagColor 白名单
- `src/core/utils.js`：safeTagColor 导出至 window.Utils
- `CHANGELOG.md`：补 v1.0.23 条目

**备注**：v1.0.22 收尾时 make-dmg 曾因版本文件读取滞后产出错误命名的 dmg（LockPass_1.0.20），已删除并以 v1.0.22 重新生成。v1.0.23 构建时发现**沙箱内 `version:set` 的写入未可靠落盘（真实磁盘版本号一度回退）**：需以提权（非沙箱）方式重跑 `version:set` 后再全量构建；`npm run build` 的 dmg 步骤（hdiutil 写 /Volumes）必须提权且在**前台**执行（后台任务不继承提权）。最终 v1.0.23 全量构建（tauri release + dmg + updater 签名）一次通过。未执行任何 git 操作。

## 复审遗留 P3 项收口 + 公钥语义修复 → v1.0.24（用户确认：公钥行内复制公钥 + P3 全部处理）

**版本**：v1.0.23 → v1.0.24（PATCH；version:set/version:check 均提权执行，11 处一致）

**修改文件清单**：
- `src/components/entries/DetailPanel.vue`：app 类型「公钥」行 `copy-mode` 改 `value`（行内复制公钥；footer 主按钮保持 App ID 语义）
- `src/components/common/ModalBase.vue`：P3-2 删除 trapHandler 的 Escape 分支与无使用方的 closeOnEsc prop，只保留 Tab 焦点陷阱；Esc 统一由 useShortcuts 分发
- `src/composables/useVault.js`：P3-11 浮动「已复制」提示 14 项内联样式收敛为 .copy-float-tip 类
- `src/styles/modal.css`：新增 .copy-float-tip；`src/styles/base.css`：新增 --z-float-tip 令牌
- `src/components/auth/AuthView.vue`：P3-9 解锁失败指数退避（≥5 次失败 1s 起翻倍封顶 30s，冷却期禁用提交+倒计时提示，仅解锁模式计数，成功/切模式复位）；P3-12 试点全量文案迁 I18n.t；P3-4 图标收敛
- `src/components/AppShell.vue`：P3-5 虚拟滚动（>100 条窗口化 ±6 行缓冲 + spacer，行高实测校准，筛选/搜索切顶）；P3-4 右键菜单/tabbar/工具栏 10 处重复图标收敛
- `src/core/utils.js`：SvgIcons 补 lock/upload/folder/alert/grid/tag
- `src/core/i18n.js`（新建，window.I18n）：zh-CN 语言包 + t() 占位符支持，注册于 main.js
- `src/components/entries/SecretFieldRow.vue` / `FieldRow.vue`、8 个 modals 的关闭按钮：重复图标收敛为 Icons.close 等
- `AGENTS.md`：目录树补 i18n.js；代码风格补 i18n 文案约定与 SvgIcons 图标约定
- `CHANGELOG.md`：补 v1.0.24 条目

**保留内联的例外**（非重复体系）：36px stroke-1.5 一次性插画（锁屏锁/空态）、tabbar FAB 2.5 描边加号、SettingsModal 主题色板白描边 check（专属视觉变体）。

**注意**：右键菜单「恢复」图标原为 refresh-ccw 风格，收敛后与卡片恢复按钮统一为 SvgIcons.restore（reply 风格）——视为一致性改进。

**验证**：npx vite build 编译通过；全量构建（提权前台）一次通过，产出 `LockPass_1.0.24_aarch64.dmg`（4.6M）+ updater 签名。注意：本次 vite 清空 dist 时触发 node 侧 safe-delete 批量删除护栏（SAFE_DELETE_BULK_CONFIRM_REQUIRED），需先 `rm -rf dist`（shell 层，不经过 node shim）再跑 `npm run build`。未执行任何 git 操作。

## 设计审计修复（P0-P3 全量）→ v1.0.25

**版本**：v1.0.24 → v1.0.25（PATCH；设计审计修复，18 项 P0-P3）

**修改文件清单**：

### P0（可访问性）
- `src/styles/base.css`：`--text-faint` 暗色 `#484f58`→`#797f8b`、浅色 `#8c959f`→`#6e7681`（WCAG AA 对比度）
- `src/components/common/ModalBase.vue`：新增 `ariaLabel` prop + `ariaLabelledBy` ref，`onMounted` 自动查找标题元素赋 id，模板加 `role="dialog" aria-modal="true" :aria-labelledby`
- `src/components/modals/ChangePwModal.vue`：三个密码字段各添加可见性切换按钮（`showOldPw`/`showNewPw`/`showConfirmPw` ref + 眼睛 toggle）；顶部新增 `lock-warning` 警告框（`role="alert"`）

### P1（操作流程）
- `src/components/modals/SettingsModal.vue`：单一长列表重构为 6 个标签页（security/appearance/sync/data/extension/about），`SETTINGS_TABS` 数组 + `role="tablist"` 标签栏 + `v-show` 绑定
- `src/styles/settings.css`：新增 `.settings-tabs` / `.settings-tab` / `.settings-tab.active` 样式
- `src/components/modals/EntryEditorModal.vue`：添加 `_initialSnapshot` + `snapshotForm()` + `hasUnsavedChanges()` + `handleClose()`（关闭时若有修改弹 confirm）；`validateForm()` 校验标题/URL/端口/邮箱；所有 `closeModal()` 替换为 `handleClose()`
- `src/components/AppShell.vue`：`onCardKeydown` 扩展 Shift+F10 和 ContextMenu 键支持
- `src/components/layout/HeaderBar.vue`：搜索框清除按钮（X 图标，`v-if="searchQuery"`）+ `clearSearch()` + Escape 先清空再失焦
- `src/styles/layout.css`：`.search-clear-btn` 样式 + input padding 调整
- `src/components/modals/ImportModal.vue`：`previewCSV()` 添加列头校验（缺 title/password 报错中止）+ 未知列名警告

### P2（UX + 视觉一致性）
- `src/core/utils.js`：`showToast()` 扩展第三参数 `options`（`action` label+callback / `duration`）
- `src/composables/useVault.js`：`softDelete()` 移除前置 confirm，直接删除后 showToast 带"撤销"按钮（5 秒有效，callback `restoreEntry(id)`）；`vaultState.srAnnounce` 字段 + `copyToClipboard()` 成功后设置
- `src/styles/modal.css`：`.toast-msg` (flex:1) + `.toast-action` 样式
- `src/components/AppShell.vue`：template 末尾 `<div class="sr-only" role="status" aria-live="polite">`
- `src/styles/utilities.css`：`.btn-icon.btn-icon-sm` 24px→28px，`.btn-icon.btn-icon-xs` 26px→24px
- `src/styles/base.css`：新增 `--radius-xs: 4px`
- 批量替换硬编码 border-radius：settings.css / entries.css / editor.css / modal.css / layout.css 中 `4px→var(--radius-xs)` / `6px→var(--radius-sm)` / `8px→var(--radius-sm)` / `10px→var(--radius-md)` / `12px→var(--radius-lg)`
- `src/styles/modal.css`：`#fff` → `var(--text-on-accent)`（copy-float-tip）；`src/styles/layout.css`：`#fff` → `var(--text-on-accent)`（tabbar badge）

### P3（功能增强）
- `src/styles/base.css`：浅色主题 `--bg-tertiary` / `--bg-hover` / `--border` / `--accent-dim` / `--accent-glow` 微调
- `src/components/modals/ExportModal.vue`：添加 `exportTagFilter` ref + `entriesToExport` computed + `availableTags` computed；两个导出函数改用 `entriesToExport.value`；模板添加 `<select>` 标签筛选下拉框
- `src/components/modals/TagsModal.vue`：新增标签合并功能——`openMergeForm(name)` + `mergeTargetTags` computed + `confirmMergeTag()`（遍历条目替换标签去重 + 删除源标签）；模板新增合并按钮 + 合并视图（源标签信息 + 目标标签下拉选择）
- `src/styles/settings.css`：`.merge-source-info` / `.merge-source-name` 样式
- `CHANGELOG.md`：补 v1.0.25 完整条目

**验证**：11 个修改文件 `@babel/parser` 语法全通过；7 个 CSS 文件花括号平衡；`npm run version:check` 11 处版本号一致。未执行任何 git 操作。
