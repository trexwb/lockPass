# 2026-09-02 工作记录

## LockPass 五轴审计修复（S1/C1/C2+A1/A2/S2/S3）→ v1.1.12

**任务来源**：全量代码审计报告（output/lockpass-code-audit-report.md）修复；Passkey 方案暂不执行。

### 必修项

1. **S1 - EntryEditorModal 草稿明文持久化** → 内存驻留
   - 新建 `src/composables/editorDraftStore.js`：纯内存 `Map` 草稿存储（loadDraft/saveDraft/clearDraft/clearAllDrafts），明文不再落任何持久化媒介。
   - `src/components/modals/EntryEditorModal.vue`：draftKey 改为内存键（'new' / 'edit:<id>'）；loadDraft/persistDraft/clearDraft 全部改走内存 store（persistDraft 额外存 customFields）；onMounted 调 clearLegacySessionDrafts 清理旧版 sessionStorage 遗留明文草稿。
   - 清理边界：保存成功 / 关闭放弃 / 右键「清空草稿」/ 锁定 / 退出登录（useVault.clearEditorDrafts → memClearAllEditorDrafts）。
   - 说明：内存同步写无 IO 安全面，不再需要防抖；如未来需跨刷新续写，必须走独立加密通道或受控数据目录，严禁明文 JSON 回写 sessionStorage/localStorage。

2. **C1 - database 类型缺 dbType/dbName 表单控件**
   - `src/components/modals/EntryEditorModal.vue`：database 模板块新增 dbType（数据库类型）/ dbName（数据库名）可编辑输入（含右键复制/清空菜单）。
   - `src/components/entries/DetailPanel.vue`：详情面板 database 区新增 dbType/dbName FieldRow 展示（copyable）。
   - `src/i18n/zh.json` / `en.json`：新增 editor.label/ph.dbType、dbName 与 detail.field.dbType/dbName。
   - 数据流：schema（TYPE_FIELD_KEYS.database）→ 编辑器控件 → saveEntry 平铺 entry.dbType/dbName → DetailPanel 展示，全链路闭合。

3. **C2+A1 - related.js 死代码 renderRelatedSection**
   - `src/core/related.js`：删除 renderRelatedSection 死代码及其导出（内联 onclick 引用未挂载 window.selectEntry 的隐患消除）；保留 getRelatedEntries 等被 Vue 组件消费的纯函数。

4. **A2 - window.* 全局桥一致性（双存储 / 全局 App）**
   - `src/core/related.js`：getRelatedEntries 内裸引用 App 改为显式判空的 getVaultState()。
   - `src/core/database.js`：DBUtils（IndexedDB 版）挂载加 `if (!window.LockTauri.isTauri)` 条件——仅浏览器环境挂载。
   - `src/core/file-store.js`：注释明确 Tauri 下为首次挂载文件版 DBUtils（非覆盖）；逻辑不变。
   - `src/main.js`：database/file-store import 处加显式条件分发注释，消除「靠 import 顺序覆盖」的隐式时序耦合。

### 低成本顺手项

5. **S2 - 锁定/退出主动清空系统剪贴板**
   - `src/composables/useVault.js`：新增 `clearClipboardNow()`（拆除自清定时器/倒计时胶囊链路幂等 + 向系统剪贴板写空串）；lockVault 与 logout 均改调 clearClipboardNow()，与 30s 自清去重。
6. **S3 - Rust 文件写权限 0600**
   - `src-tauri/src/lib.rs`：`atomic_write`（vault.json/meta.json 落盘通道）与 `export_text_file`（导出备份）在写文件后显式 `#[cfg(unix)] set_permissions(0o600)`，防同机其他用户读取加密数据文件。
7. **C6/P1（快捷键 defs 顶层缓存）—— 评估后未改**
   - `useShortcuts.js`：buildShortcutDefs 在 useShortcuts() 实例内已一次性构建（非按键热路径重复构建）；顶层缓存需固定 useVault 实例函数引用，引入旧实例耦合风险，收益 < 风险，维持现状。
8. **C4（window.App 命名收敛）—— 风险高跳过**
   - window.App 引用分散在 import-bridge.js(7)/useVault.js(6)/related.js(3)，收敛为统一命名需跨 core/composables 层替换并复核 boot 前裸访问时序，本次跳过（挂载点已有显式判空守卫）。

### 验证

- `npm run vite:build`：70 modules transformed，built in 282ms，0 error。
- 改动 JS 语法：`node --check`（ESM 自动识别）全部 OK：editorDraftStore.js / related.js / database.js / file-store.js / main.js / useVault.js。
- i18n JSON：JSON.parse 校验通过（zh/en）。
- Rust：`cargo check` Finished dev profile，0 error。
- `npm run version:set 1.1.12` + `npm run version:check`：11 处一致（v1.1.12）。
- 未执行任何 git add/commit/push/tag。

---

## S1 分级缓存策略补强（同日第二轮 · 仍在 v1.1.12）

**任务来源**：用户新定 S1 策略 → 草稿改为【分级缓存】：仅非敏感元数据写 sessionStorage（脱敏子集，跨刷新可恢复骨架），敏感字段一律不落盘仅内存；关闭/锁屏清内存敏感字段，非敏感落盘加防抖；恢复按脱敏子集处理。

1. `src/composables/editorDraftStore.js`：从纯内存 Map 重构为「内存全量 + sessionStorage 脱敏子集」双轨。
   - SAFE_PREFIX=`lockpass_safe_draft_`；SAFE_FIELD_KEYS 白名单：title/entryType/tags/notes/url/username/port/dbType/dbName/appId/rootUser 等（依 ENTRY_TYPES/TYPE_FIELD_KEYS 实际 schema）。
   - SENSITIVE_TERMS（label 命中：password/pwd/token/api/key/secret/seed/recovery/otp/...）+ VALUE_SENSITIVE_RE（value 命中强正则）双判定；自定义字段 value 语义未知默认不缓存；sensitive 标记项（pin/otp 类型）不落盘。
   - toSafeSubset 只产安全骨架；300ms 防抖落盘；loadDraft 内存 miss 回退读脱敏子集，含敏感载荷/旧格式一律拒读并清除；clearDraft 清内存+storage；clearAllDrafts 仅清内存（锁屏/登出，脱敏骨架保留）。
   - 新增导出 `hasInMemoryDraft(key)`：组件区分「内存全量（整组替换）」vs「session 脱敏（按 id 合并）」。
2. `src/components/modals/EntryEditorModal.vue`：自定义字段恢复按来源分流——内存全量整组替换；session 脱敏子集按 id 合并，不清除编辑条目已保存的敏感自定义项。
3. **清理遗留明文预填入口**（grep 发现三处仍直写 `lockpass_draft_new` 明文 JSON 且与 onMounted 清理冲突）：
   - `src/components/entries/DetailPanel.vue` 右键「复制为草稿」→ `memSaveDraft('new', …)`。
   - `src/components/AppShell.vue` 列表右键「复制为草稿」→ `memSaveDraft('new', …)`。
   - `src/components/layout/SidebarNav.vue` 按类型新建预选 → 打开前 `memLoadDraft + memSaveDraft` 写入 entryType（修复时序并消除明文键依赖）。
4. `src/composables/useVault.js`：注释与清理路径保持（clearEditorDrafts 只清内存全量明文）。

**验证**：`node --check` 通过；`vite build` 70 modules 0 error（298ms）；临时行为脚本 `temp/verify_s1_tiered.mjs` 全链路断言通过（敏感不落盘/元数据恢复/旧格式拒读/防抖 clear 无残留）；`version:set 1.1.12` + `version:check` 11 处一致（幂等复验）；报告更新至 `output/lockpass-audit-fix-report.md`。未执行任何 git 操作。

---

## 草稿生命周期重构（v1.1.12b 行为规则 · 同日第三轮 · 仍为 v1.1.12 不升版）

**任务来源**：用户反馈「关闭窗口即清空」不符合草稿语义，确立新规则：草稿作用 = 未提交前持续保留。仅提交并真实保存成功才清草稿；任何关闭 / 刷新 / 跳转 / 切换不清空；进入新建表单检测到未提交草稿弹提示询问（使用则继续编辑、不使用才清空并展示空白表单）；编辑已有条目一致处理（有差异草稿提示恢复、与已保存一致不打扰）；提交失败保留以便重试；锁屏 / 登出维持 S1 基线（仅清内存明文、保留脱敏骨架）。本轮不升版本号（v1.1.12，未执行 version:set / git）。

1. `src/composables/editorDraftStore.js`：文件头注释同步 v1.1.12b 语义；确认 _pendingDrafts Map + flushDrafts() + pagehide/beforeunload 兜底 + isEmptyDraft() + draftsEqual() 齐备（关闭不清空后的刷新前兜底落盘、空骨架/一致草稿不打扰判定基础）。
2. `src/composables/useVault.js`：vaultState 新增 `editorOpenOpts`（打开意图：presetType / draftAction，closeModal 置空）；`saveVault()` 返回 Promise<boolean>（真实落盘成败，合并写链 flushSaveResolvers resolve lastSaveOk）；`saveEntry()` 重构为：变更前快照 → mutate → await saveVault()，失败回滚内存并返回 false（草稿保留可重试，避免失败后关闭丢失与重复条目/重复历史），成功才 memClearEditorDraft(对应键) + 移除旧 lockpass_draft_* 明文残留，再 closeModal + toast。
3. `src/components/modals/EntryEditorModal.vue`：
   - 生命周期主流程 onMounted：先 clearLegacySessionDrafts() 清旧明文残留；读 editorOpenOpts——编辑场景先 fillFromEntry(e) 装载已保存内容，再按草稿判空/判等决定是否 askDraftUse（一致不打扰）；新建场景 presetType 预选 → draft 非空则分「draftAction:'use' 直用（复制流）」「普通询问（规则②）」。
   - _mounting 装载保护（装载/预选/applyDraft 期间不写草稿，真实交互才 persistDraft）；finalizeMount 预置空字段、updateStrength、记录 _initialSnapshot。
   - applyDraft 自定义字段双来源恢复：内存全量整组替换 / session 脱敏按 id 合并（保留已保存敏感项）。
   - handleClose / 取消按钮 / ModalBase @close 一律 closeModal 不清草稿；右键「清空草稿」为显式清空通道；onSave 不再自行清草稿（统一交由 saveEntry 落盘成功后处理）。
   - askDraftUse：confirm=applyDraft+toast「已恢复」；cancel=clearDraft()+toast「已清空」+空白表单（新建）/保持已保存（编辑）。
4. 入口收敛复核：DetailPanel / AppShell「复制为草稿」memSaveDraft('new') 先写后 openEntryModal(null,{draftAction:'use'})（不重复询问）；SidebarNav 类型预选 openEntryModal(null,{presetType})（空骨架草稿不再误触发询问）；三入口均不再直写明文 sessionStorage。
5. i18n：zh/en 新增 editor.confirmDraftTitle/MsgNew/MsgEdit/Use/Discard + toastDraftCleared/toastDraftRestored 键。
6. 文档校正：docs/version/RELEASE-v1.1.md v1.1.12 分节 S1 描述中「保存/关闭/锁屏/登出清空内存明文」改为 v1.1.12b 行为规则全量表述，验证段补生命周期 store 断言行；README.md 无版本索引行、无需同步（已核查）。

**验证**：`vite build` 70 modules 0 error（284ms）；改动 JS `node --check` 通过（editorDraftStore / useVault）；store 规则断言脚本 `temp/verify_draft_lifecycle.mjs` 全链路通过（A 敏感仅内存、B 关闭重开可恢复、C flush 最新骨架、D 保存成功清空、E 空骨架/一致不打扰、F 锁屏仅清内存明文保留骨架、G 旧键空间隔离）；未执行 version:set / git 操作。

---

## 更新签名验证失败根因排查（v1.1.13 · 未修复，仅诊断）

**现象**：客户端更新报 `更新下载失败：The signature verification failed`。

**根因**：签名私钥与客户端内置公钥不是同一对 minisign 密钥。经解码比对 key ID：

| 对象 | 位置 | key ID |
|------|------|--------|
| 客户端内置公钥 | `src-tauri/tauri.conf.json` → `plugins.updater.pubkey` | `C76F69519116440E` |
| 本地公钥 | `~/.tauri/lockpass-updater.key.pub` | `C76F69519116440E` ✅ |
| **本地私钥** | `~/.tauri/lockpass-updater.key` | **`0157AB3832426353`** ❌ |

- `.env.local` 的 `TAURI_SIGNING_PRIVATE_KEY_PATH` 指向 `~/.tauri/lockpass-updater.key`（0157… 错误私钥）。
- `.key`（私钥）与 `.key.pub`（公钥）虽同为 8/28 12:30 生成，但 key ID 不同 → **私钥文件被替换/重新生成过，公钥未同步**。
- 线上已发布 latest.json（tag `v1.1`，version 1.1.12，2026-09-02 07:51 发布）的 signature key ID = `C76F…`（正确），说明当时 CI 用 GitHub Secrets 里的 C76F… 私钥签名，与内置公钥匹配。
- 附带发现：线上 release tag 为 `v1.1`，但 `scripts/gen-latest-json.mjs` 生成 url 用 `v + version` = `v1.1.12`，tag 与 url 不一致（潜在 404 隐患，与本签名报错无直接因果）。

**修复方向（二选一，尚未执行）**：
- A（推荐，若 C76F… 私钥仍在 GitHub Secrets）：从 Secrets 取回 C76F… 私钥，覆盖本地 `~/.tauri/lockpass-updater.key`。
- B（私钥已丢失）：`tauri signer generate` 重新生成密钥对，同步更新 `tauri.conf.json` pubkey + 本地私钥 + GitHub Secrets（旧客户端将无法再自动更新）。

**诊断方法（minisign key ID 提取，可复用）**：私钥/公钥/`.sig` 均为整体 base64 单行，解码后第二行主体 `bytes[2:10]` 为 8 字节 key ID（小端），`bytes[::-1].hex().upper()` 即官方 key ID。未执行任何 git/修复操作。

---

## 更新签名失败根因 · 决定性修正（同日 · 私钥 key ID 实证）

**关键修正**：上一节「本地私钥 = `0157AB3832426353`」是**误读**。加密私钥（`rsign encrypted secret key`）容器的 `bytes[2:10]` 并非真实签名 key ID，不能这么读。

**实证**：`npx tauri signer sign -f ~/.tauri/lockpass-updater.key -p <密码> /tmp/xx.txt` 实际签名成功，解码签名主体 `bytes[2:10]` → 真实 key ID = **`C76F69519116440E`**。

**结论（当前状态）**：
- 本地私钥 `~/.tauri/lockpass-updater.key` = **C76F**（mtime 8/28 12:30 从未变过，一直是这把）。
- 用户 9/2 16:55「找回公钥」把 `tauri.conf.json` 与 `.key.pub` 改成 **`D83BAF15C3843101`**（8/28 11:50 `9d8993c` v1.0.14 时代旧公钥）。
- 正确公钥 = **C76F**（8/28 13:56 `7f2c3c9` v1.0.19 起持续使用）。

**推论**：
1. 「公钥私钥对不对得上」→ **没对上**（找回的 D83B 是旧钥匙）。
2. 更关键：**改动前（16:55 前）tauri.conf.json/.key.pub 都是 C76F，与私钥 C76F 匹配**。故 1.1.11 签名失败根因大概率**不是**本地公钥私钥不匹配（本地私钥根本不参与线上 CI 签名；线上 v1.1.x 产物签名也是 C76F）。「找回公钥」反而把匹配态改成了不匹配。

**待办**：① 恢复公钥为 C76F（tauri.conf.json + .key.pub）；② 重查 1.1.11 真正根因（候选：客户端构建时内置公钥是否确为 C76F、线上 latest.json signature key ID、tag `v1.1` 与 url `v1.1.12` 不一致、产物损坏等）。

**正确公钥值（key ID C76F69519116440E）**：
`dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IEM3NkY2OTUxOTExNjQ0MEUKUldRT1JCYVJVV2x2eHc4REpnM3E5VXRjVENyUkR5eitNY3VlU3RYenhuem5xTDBsRU81MXF5Q04K`

---

## 版本重置 v1.0.0 + 密钥对重新生成（同日 · 用户决策：大胆改版）

**任务来源**：用户决定所有版本号回归 v1.0.0（含 CHANGELOG），重新生成密钥对，从 1.0.0 重新发布。三项确认：备份后覆盖 / 沿用旧密码 / CHANGELOG 清空重写。

**已执行**：
1. **备份旧密钥对**：`~/.tauri/lockpass-updater.key` / `.key.pub` → `.bak`（旧 C76F 私钥保留可回退）。
2. **重新生成密钥对**：`npx tauri signer generate --ci -p <沿用旧密码> -w ~/.tauri/lockpass-updater.key -f`。
   - 新公钥 key ID = **`710B0591EEA6E813`**；配对自检通过（新私钥签名 key ID == 新公钥 key ID）。
3. **更新 tauri.conf.json pubkey** → 新公钥 `710B0591EEA6E813`。
4. **版本号回归**：`npm run version:set 1.0.0` 统一 8 文件；`version:check` 11 处一致（v1.0.0）。
   - 覆盖：package.json / package-lock.json / tauri.conf.json / extension/manifest.json / Cargo.toml / AGENTS.md / docs/spec.md（+ 注入模式 3 处校验）。
5. **CHANGELOG.md 清空重写**：删除全部历史，仅保留一个 v1.0.0 条目。

**待用户操作（AI 不执行 git / 外部发布）**：
- **GitHub Secrets 更新**：`TAURI_SIGNING_PRIVATE_KEY`（新私钥内联 base64 全文，即 `~/.tauri/lockpass-updater.key` 内容）；密码沿用旧值，`TAURI_SIGNING_PRIVATE_KEY_PASSWORD` 若已设则无需改。
- **git 发布**：提交 → 打 tag `v1.0.0`（⚠️ 历史已有同名 v1.0.0 tag，需先删旧 tag 或改用带后缀 tag）→ 推送 → GitHub release → 上传产物 + latest.json。
- **提醒**：旧客户端（内置 C76F 公钥）无法通过 updater 自动更新到新版本，需手动重装。

**新公钥值（key ID 710B0591EEA6E813）**：
`dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDcxMEIwNTkxRUVBNkU4MTMKUldRVDZLYnVrUVVMY1drSFNITTRLaG1BOFJiQnhPRkgvMkZHUS9kcThsWHBnK3A2Qi81VXFSRUMK`


---

## 生物识别解锁（Passkey 方案 A · v1.1.12 新增功能）

**范围**：macOS 桌面单端 MVP，主密码仍为根信任。启用（enroll）/解锁（unlock）/停用（remove）/状态（status）四 command 全链路落地。

**Rust**（`src-tauri/src/passkey.rs`，lib.rs 注册 passkey_status/enroll/unlock/remove）：
- 启用时新建 Secure Enclave ECIES 密钥对存 Keychain（tokenID=com.apple.setoken、synchronizable=false、数据保护钥匙串、访问控制 userPresence|BiometryCurrentSet|PrivateKeyUsage、AccessibleWhenUnlockedThisDeviceOnly）；公钥加密 32 字节 Device Unlock Key（实为前端按保险箱 salt/iterations 派生出的当前 Vault Key raw）写 guard `passkey_guard.json`（0600 原子写）。
- 解锁触发系统生物验证，通过后 SE 私钥解密 guard 返回 Device Key hex；未配置/取消/失败返回 `LKPK:<CODE>:<detail>`（USER_CANCELED/AUTH_FAILED/KEYCHAIN_ERR/CRYPTO_ERR/FILE_ERR/ALREADY_ENABLED/NOT_ENABLED/UNSUPPORTED），禁止静默降级。
- 停用删 Keychain item + guard；enabled = guard 与 item 同时存在。

**前端**：
- `src/core/crypto.js`：+deriveKeyBytes/importRawAesKey/bytesToHex（挂 window.CryptoUtils）。
- `src/core/passkey-bridge.js`：window.LockPasskey（isDesktopMac 判定 + 4 方法 + LKPK 错误归一），main.js 注册。
- `useVault.js`：+handleBiometricUnlock——importRawAesKey（extractable=false 仅内存）解密主库后与主密码解锁同语义；**生物会话不写 sessionPassword**（锁屏即随 cryptoKey 清空，依赖主密码功能提示先用主密码）。getSession 供设置页启用开关。
- `AuthView.vue`：已启用时显示「生物识别解锁」按钮（available&&enabled）。
- `SettingsModal.vue` 安全页：macOS 桌面显示生物识别开关；启用需主密码已解锁会话（会话主密码按 DBUtils.META salt/iterations 派生 32B hex 传 enroll）；停用直接 remove。
- i18n zh/en：lock.bioUnlock / vault.lock.errBio.* / settings.security.bio*（含 KEY_MISMATCH 指引改主密码后重启用）。

**设计说明**：`docs/passkey.md`。

**验证**：cargo check 0 error 0 warning；vite build 0 error（71 modules）；node --check JS 全过。
**版本**：未执行 version:set（保持 v1.1.12，MINOR 升版由用户决定）；未执行 git。
**未做**：Windows/Linux 仅为 UNSUPPORTED stub；改主密码后生物记录需手动重启用（KEY_MISMATCH 引导）；BiometryCurrentSet 语义（重录指纹后需重启用）。
