# 2026-09-02 工作记录

## LockPass 五轴审计修复（S1/C1/C2+A1/A2/S2/S3）→ v1.1.12

**任务来源**：全量代码审计报告（output/lockpass-code-audit-report.md）修复；Passkey 方案暂不执行。

### 必修项

1. **S1 - EntryEditorModal 草稿明文持久化** → 内存驻留
   - 新建 `src/composables/editorDraftStore.js`：纯内存 `Map` 草稿存储（loadDraft/saveDraft/clearDraft/clearAllDrafts），明文不再落任何持久化媒介。
   - `src/components/modals/EntryEditorModal.vue`：draftKey 改为内存键（'new' / 'edit:<id>'）；loadDraft/persistDraft/clearDraft 全部改走内存 store（persistDraft 额外存 customFields）；onMounted 调 clearLegacySessionDrafts 清理旧版 sessionStorage 遗留明文草稿。
   - 清理边界：保存成功 / 关闭放弃 / 右键「清空草稿」/ 锁定 / 退出登录（useVault.clearEditorDrafts → memClearAllEditorDrafts）。
   - 说明：内存同步写无 IO 安全面，不再需要防抖；如未来需跨刷新续写，必须走独立加密通道或受控数据目录，严禁明文 JSON 回写 sessionStorage/localStorage。

2. **C1 - database 类型缺 dbType/dbName 表单控件**
   - `src/components/modals/EntryEditorModal.vue`：database 模板块新增 dbType（数据库类型）/ dbName（数据库名）可编辑输入（含右键复制/清空菜单）。
   - `src/components/entries/DetailPanel.vue`：详情面板 database 区新增 dbType/dbName FieldRow 展示（copyable）。
   - `src/i18n/zh.json` / `en.json`：新增 editor.label/ph.dbType、dbName 与 detail.field.dbType/dbName。
   - 数据流：schema（TYPE_FIELD_KEYS.database）→ 编辑器控件 → saveEntry 平铺 entry.dbType/dbName → DetailPanel 展示，全链路闭合。

3. **C2+A1 - related.js 死代码 renderRelatedSection**
   - `src/core/related.js`：删除 renderRelatedSection 死代码及其导出（内联 onclick 引用未挂载 window.selectEntry 的隐患消除）；保留 getRelatedEntries 等被 Vue 组件消费的纯函数。

4. **A2 - window.* 全局桥一致性（双存储 / 全局 App）**
   - `src/core/related.js`：getRelatedEntries 内裸引用 App 改为显式判空的 getVaultState()。
   - `src/core/database.js`：DBUtils（IndexedDB 版）挂载加 `if (!window.LockTauri.isTauri)` 条件——仅浏览器环境挂载。
   - `src/core/file-store.js`：注释明确 Tauri 下为首次挂载文件版 DBUtils（非覆盖）；逻辑不变。
   - `src/main.js`：database/file-store import 处加显式条件分发注释，消除「靠 import 顺序覆盖」的隐式时序耦合。

### 低成本顺手项

5. **S2 - 锁定/退出主动清空系统剪贴板**
   - `src/composables/useVault.js`：新增 `clearClipboardNow()`（拆除自清定时器/倒计时胶囊链路幂等 + 向系统剪贴板写空串）；lockVault 与 logout 均改调 clearClipboardNow()，与 30s 自清去重。
6. **S3 - Rust 文件写权限 0600**
   - `src-tauri/src/lib.rs`：`atomic_write`（vault.json/meta.json 落盘通道）与 `export_text_file`（导出备份）在写文件后显式 `#[cfg(unix)] set_permissions(0o600)`，防同机其他用户读取加密数据文件。
7. **C6/P1（快捷键 defs 顶层缓存）—— 评估后未改**
   - `useShortcuts.js`：buildShortcutDefs 在 useShortcuts() 实例内已一次性构建（非按键热路径重复构建）；顶层缓存需固定 useVault 实例函数引用，引入旧实例耦合风险，收益 < 风险，维持现状。
8. **C4（window.App 命名收敛）—— 风险高跳过**
   - window.App 引用分散在 import-bridge.js(7)/useVault.js(6)/related.js(3)，收敛为统一命名需跨 core/composables 层替换并复核 boot 前裸访问时序，本次跳过（挂载点已有显式判空守卫）。

### 验证

- `npm run vite:build`：70 modules transformed，built in 282ms，0 error。
- 改动 JS 语法：`node --check`（ESM 自动识别）全部 OK：editorDraftStore.js / related.js / database.js / file-store.js / main.js / useVault.js。
- i18n JSON：JSON.parse 校验通过（zh/en）。
- Rust：`cargo check` Finished dev profile，0 error。
- `npm run version:set 1.1.12` + `npm run version:check`：11 处一致（v1.1.12）。
- 未执行任何 git add/commit/push/tag。
