# 2026-08-28 工作记录

## 代码审计与修复 v1.0.20

### 审计范围
- 全面代码审计 LockPass 项目（38 文件 / ~9258 行）
- 覆盖 7 个维度：安全/正确性/性能/可维护性/测试/可访问性/文档

### 修复清单（12 项，版本升至 v1.0.20）

#### CRITICAL（4 项）
1. `tauri-bridge.js:88` — `lt.invoke` → `LT.invoke`（macOS 剪贴板清除失效）
2. `ext-bridge.js:32` — postMessage `'*'` → `window.location.origin`（密码泄露）
3. `crypto.js` — PBKDF2 迭代次数 100000 → 600000（OWASP 2023 推荐），新增 `DEFAULT_ITERATIONS` / `LEGACY_ITERATIONS` 常量
4. `ChangePwModal.vue` — 重新加密时遗漏 `history` 字段，已加入 payload；修改密码时自动升级迭代次数

#### MAJOR（4 项）
5. `SettingsModal.vue` — setInterval 内存泄漏，添加 `onBeforeUnmount` 清理
6. `useVault.js` — 剪贴板倒计时 interval 累积，改为 `vaultState.clipboardNoteTimer` 统一管理
7. `EntryEditorModal.vue` — 切换 entryType 时旧字段残留，添加 watch 清理
8. `useVault.js` + `DetailPanel.vue` — `showPassword` 从 entry 对象移至 `vaultState.showPasswordMap`

#### MINOR（4 项）
9. `tauri-server-bridge.js` — var → const/let 统一
10. `vite.config.js` — esbuild `pure` 配置移除生产构建的 console.debug/log
11. `DetailPanel.vue` + `ModalBase.vue` — aria-modal / aria-label / 键盘可访问性
12. 创建 `CHANGELOG.md`

### 未修复（有意跳过）
- #13 QrShareModal 复用 cryptoKey：安全设计决策，保持独立 salt 隔离
- #14 useVault.js 拆分：风险过大，需测试覆盖后逐步重构
- #8 零测试覆盖：需单独建立测试框架
