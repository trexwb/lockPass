# LockPass — 个人密码工作台 规格文档

> 版本：v1.0.7 | 更新日期：2026-08-21

---

## 1. 产品定位

一款运行在浏览器中的离线密码保险箱。视觉上借鉴实体保险箱的金属质感与机械密码锁的精密感，配合柔和的发光提示，营造「数字保险箱」的氛围。数据完全存储在本地 IndexedDB 中，永不联网——就像把一张写满密码的纸锁进抽屉，而不是交给云端。

**核心体验原则：安全 → 简洁 → 顺手**

---

## 2. 技术选型

### 2.1 存储方案对比

| 维度 | IndexedDB | sql.js (SQLite WASM) |
|------|-----------|---------------------|
| **数据容量** | 数百 MB ~ GB，浏览器原生 | 需加载整个 DB 到内存，大文件性能下降 |
| **存储持久化** | 浏览器自动持久化 | 需手动导入/导出 `.db` 文件才能持久化 |
| **离线可用** | ✅ 完全离线 | ✅ 完全离线 |
| **初始化开销** | 原生 API，零加载 | 需加载 ~1MB WASM 文件 |
| **查询能力** | 索引 + 游标，足够 | 完整 SQL，更强 |
| **兼容性** | 所有现代浏览器 | 所有现代浏览器 |
| **典型场景** | 结构化数据持久存储 | 需要复杂 SQL 查询 / 分析型应用 |

**结论：选择 IndexedDB**

理由：
1. 密码管理器数据量有限（几十到几千条），IndexedDB 索引查询已足够
2. 自动持久化，无需用户手动保存 `.db` 文件
3. 零依赖，浏览器原生 API，无需加载额外 WASM
4. 容量上限高，Chrome 单库可达可用磁盘空间的 60%

### 2.2 加密方案

```
主密码 → PBKDF2(SHA-256, 100000次迭代, 32字节盐值) → AES-256-GCM 密钥
数据   → AES-256-GCM 加密（随机12字节IV）          → 密文存储
```

**选择理由**：
- **PBKDF2**：行业标准密钥派生函数，NIST 推荐
- **100000 次迭代**：抵御暴力破解，同时保持合理的解锁时间（~100ms）
- **AES-256-GCM**：提供加密 + 完整性校验，NIST 推荐

### 2.3 技术栈

- **前端框架**：纯 Vanilla JavaScript（无框架依赖）
- **存储**：IndexedDB（浏览器原生）
- **加密**：Web Crypto API（浏览器原生）
- **UI**：CSS Variables + Flexbox/Grid
- **字体**：system-ui 系统字体栈（零外部依赖，纯离线）

---

## 3. 功能规格

### 3.1 主密码与保险箱初始化

| 功能 | 说明 |
|------|------|
| 首次使用 | 引导创建主密码（8位以上，建议强密码） |
| 主密码派生 | PBKDF2（SHA-256，100000次迭代）派生 AES-256-GCM 密钥 |
| 盐值生成 | 随机生成 32 字节盐值，存储在 IndexedDB 中 |
| 数据加密 | 条目列表以 AES-256-GCM 加密后存入 IndexedDB |

### 3.2 解锁流程

```
输入主密码 → PBKDF2 派生密钥 → 尝试解密数据 → 成功则解锁
```

- 解锁超时：5分钟无操作自动锁定（可配置）
- 解锁失败：显示错误，抖动动画

### 3.3 密码条目管理

每条密码条目包含：

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| id | UUID | 自动 | 唯一标识 |
| title | String(100) | ✅ | 标题 |
| username | String | | 用户名/账号 |
| password | String | ✅ | 密码（加密存储） |
| url | String | | 网站地址 |
| tags | String[] | | 标签数组（支持多标签） |
| favorite | Boolean | | 是否收藏 |
| notes | String(256) | | 备注 |
| createdAt | ISO8601 | 自动 | 创建时间 |
| updatedAt | ISO8601 | 自动 | 更新时间 |

### 3.4 标签管理

- 默认推荐标签：社交、邮箱、金融、工作、开发、生活、其他（共 7 个）
- 每个密码条目可关联多个标签
- 支持增/删/改标签名称、颜色和图标
- 侧边栏显示热门标签（按使用频率取前 8 个），点击筛选

### 3.5 搜索

- 实时全文搜索（标题/用户名/URL/标签）
- 搜索高亮匹配词
- 无结果时显示空状态

### 3.6 密码生成器

- 长度滑块：8~64位（默认16位）
- 字符集选项：大写字母/小写字母/数字/符号
- 可排除歧义字符（0/O/l/1/I）
- 实时预览生成结果
- 密码强度实时显示（Entropy 熵值计算）

### 3.7 密码操作

- 点击眼睛图标：显示/隐藏密码
- 点击复制：复制密码到剪贴板，3秒后自动清空剪贴板
- 右键条目卡片：快捷菜单（编辑/复制/删除/收藏）

### 3.8 数据导入导出

#### 导出格式

| 格式 | 说明 | 安全性 |
|------|------|:------:|
| `.vault` | 加密 JSON 文件（包含盐值+密文+IV） | ✅ 安全 |
| `.csv` | 明文表格文件 | ⚠️ 需妥善保管 |

#### 导出文件结构（.vault 格式）

```json
{
  "version": 1,
  "exportedAt": "2026-08-18T10:00:00.000Z",
  "format": "encrypted",
  "salt": "base64-encoded-salt",
  "iterations": 100000,
  "iv": "base64-encoded-iv",
  "data": "base64-encoded-ciphertext",
  "tagDefs": {...}
}
```

#### 导入支持

- 加密 `.vault` 文件（需要原主密码解密）
- 明文 CSV 文件
- 导入时自动去重（按标题+用户名匹配）

### 3.9 设置

- 修改主密码（需验证原密码）
- 自动锁定时间（1/5/15/30分钟/从不）
- 复制后清除剪贴板时间（10/30/60秒）
- 标签管理（增/删/改标签名称、颜色、图标）
- 数据备份与恢复
- 销毁保险箱（彻底删除所有数据）

### 3.10 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl/Cmd + K` | 打开快速搜索 |
| `Ctrl/Cmd + N` | 新建条目 |
| `Ctrl/Cmd + L` | 锁定保险箱 |
| `Escape` | 关闭模态框/面板 |
| `Ctrl/Cmd + ,` | 打开设置 |

---

## 4. 数据结构

### 4.1 存储结构

**浏览器版（IndexedDB）**

```
Database: PasswordVaultDB

ObjectStore: meta (keyPath: key)
  { key: "salt",       value: "base64-encoded-salt" }
  { key: "iterations", value: 100000 }
  { key: "version",    value: 1 }

ObjectStore: vault (keyPath: id)
  { id: "main", iv: "base64-iv", data: "base64-aes-gcm-ciphertext" }
```

**Tauri 桌面版（本地文件存储，结构等价）**

数据目录：系统应用数据目录（macOS `~/Library/Application Support/com.lockpass`，Windows `%APPDATA%\com.lockpass`），通过 Rust 命令 `file_store_*` 读写，前端 js/file-store.js 以同接口 DBUtils 透明替换 IndexedDB：

```
<数据目录>/
├── meta.json     # 等价 meta store：{ "salt": {...}, "iterations": {...}, ... }
└── vault.json    # 等价 vault store：{ "main": { id, iv, data } }
```

加密负载结构不变（salt/iv/data 均为 base64 密文），两种存储可无缝迁移。

### 4.2 加密流程

```
1. 用户输入主密码
   ↓
2. PBKDF2(salt, password, 100000, SHA-256) → AES Key
   ↓
3. 条目数据序列化为 JSON
   ↓
4. AES-256-GCM 加密（随机 IV）
   ↓
5. 存储至 IndexedDB
```

### 4.3 密码强度计算

```
Entropy = log2(charset_size ^ length)

- 小写字母: 26
- 大写字母: 26
- 数字: 10
- 符号: 32

弱:   < 40 bits  → 红色
中:   40-60 bits → 黄色
强:   60-80 bits → 蓝色
极强: > 80 bits  → 绿色
```

---

## 5. 安全机制

### 5.1 Clipboard 安全

- 使用 `navigator.clipboard.writeText()`
- 复制后设置定时器，指定时间后写入空字符串覆盖

### 5.2 自动锁定

- 监听用户交互事件（mousemove, keydown, click, scroll）
- 重置计时器
- 超时触发 `lock()` 函数，清除内存中的明文数据

### 5.3 安全风险与防范

| 风险 | 说明 | 防范措施 |
|------|------|----------|
| XSS 攻击 | 恶意脚本窃取内存中的明文密码 | 仅在受信任环境使用，避免安装可疑浏览器扩展 |
| 浏览器漏洞 | 浏览器被入侵可能读取内存 | 保持浏览器更新，使用主流浏览器 |
| 本地文件泄露 | 电脑被盗/被入侵 | 设置电脑登录密码，启用磁盘加密 |
| 主密码遗忘 | 无法恢复数据 | 牢记主密码，或将主密码存储在安全的地方 |

---

## 6. 浏览器兼容性

| 浏览器 | 版本要求 | 说明 |
|--------|----------|------|
| Chrome | 60+ | 推荐 |
| Firefox | 60+ | 推荐 |
| Safari | 12+ | 支持 |
| Edge | 79+ | 推荐 |

---

## 7. 项目结构

```
LockPass/
├── index.html     # 入口（模块化 script 按序加载）
├── css/main.css   # 样式
├── js/            # 业务模块（crypto/database/file-store/file-sync/generator/utils/...）
├── assets/        # favicon、vendor 库
├── scripts/       # 构建辅助（copy-frontend、make-dmg、gen-icons）
├── src-tauri/     # Tauri v2 桌面封装（Rust 命令 + 图标 + 打包配置）
├── SPEC.md        # 产品规格文档（本文件）
└── README.md      # 使用说明
```

**多端形态**：
- 浏览器：双击 index.html 即用，IndexedDB 存储
- Tauri 桌面：同前端代码，本地文件存储（见 4.1），导出走系统对话框

---

## 8. 未来规划
- [ ] 支持多语言（英文/中文切换）
- [ ] 支持密码历史记录
- [ ] 支持密码过期提醒
- [ ] 支持自定义主题色
- [ ] 支持浏览器扩展（自动填充）
- [ ] 支持 TOTP 两步验证

---

**文档版本：v1.0.7**
