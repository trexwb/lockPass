/* ═══════════════════════════════════════════════════════════════════
   LockPass — 引导标记（必须最先加载，同步执行）
   Rust 侧冷启动自愈探针（lib.rs）依据 window.__LOCKPASS_BOOTED__
   判断首屏是否成功进入应用；未进入则自动 reload 重试，
   替代用户手动点「刷新」。
   ═══════════════════════════════════════════════════════════════════ */
window.__LOCKPASS_BOOTED__ = true
